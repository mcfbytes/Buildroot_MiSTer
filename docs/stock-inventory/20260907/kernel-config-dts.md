# Kernel config + DTS: regeneration (item f)

> Generated by `scripts/inventory/gen-kernel-config-dts.sh` (P0.3). Do not hand-edit — re-run the script.
>
> Evidence method: python3 (scripts/inventory/kernel_extract.py + lz4_legacy.py) to pull the IKCONFIG .config and the appended DTB out of a zImage_dtb; dtc -I dtb -O dts for the DTS text.
> dtc version used for this run: Version: DTC 1.7.0
> Regenerated from: `$S/lic/zImage_dtb`, `MiSTer-devel/Linux_Image_creator_MiSTer` commit
> `d4e3f51ec7fdd18116562d38bace9ef7dffe0f38` ("Release 20260907."), sha256
> `0bec449e365e757d14711bead76c5b1805d2b613a39e633fe7dc9f08e5687e73` (matches
> `docs/kernel-recon/fork-sync-2026-09/PLAN.md` §1.1)

Regeneration script: `scripts/inventory/gen-kernel-config-dts.sh <zImage_dtb> [output-dir]`.
This run passed `output-dir=docs/stock-inventory/20260907` explicitly, so
`stock-linux.config` and `stock.dts` in this directory ARE the freshly regenerated,
committed files for Release 20260907 (not a scratch/dry-run copy).

## Verification result (this run)

The script's own "verification" step diffs its output against
`docs/stock-inventory/20260907/stock-linux.config` / `stock.dts` (the top-level, undated path) — that
path no longer holds any file (the 5.15 copies moved to `docs/stock-inventory/20250402/`,
and nothing is checked in at the top level), so both compares report "no committed file to
compare against". That is expected and not a problem; see the real cross-release diff below.

- [stock-linux.config] no committed file at docs/stock-inventory/20260907/stock-linux.config to compare against
- [stock.dts] no committed file at docs/stock-inventory/20260907/stock.dts to compare against

## stock-linux.config: 20250402 → 20260907

`diff docs/stock-inventory/20250402/stock-linux.config docs/stock-inventory/20260907/stock-linux.config`:
4,246 → 4,659 lines, **1,163 changed lines** (added/removed/changed `CONFIG_*` lines
combined). This is the kernel-version jump itself (5.15 → 6.18) plus whatever config
changes shipped with it; a symbol-level audit against the fork's `MiSTer_defconfig` is
`docs/kernel-config-deltas.md`'s job (§9.2 of the fork-sync plan), not this inventory.

## stock.dts: 20250402 → 20260907 — a real content difference, not cosmetic

Unlike a same-kernel-version re-run (where the only expected difference is dtc
rendering — see `scripts/inventory/normalize-dts.py`), this is a genuine content diff: the
DTS grew from 998 to 1,130 lines and the node/property content itself changed (new
phandles, reordered/renamed nodes), consistent with mainline's DE10-Nano `.dts` moving
between the 5.15 and 6.18 kernel lines. `dtc` also emitted 10 `unit_address_vs_reg` /
`simple_bus_reg` warnings on the 20260907 DTB that are not present in the 20250402 one
(both are non-fatal `dtc` lint warnings, not compile failures). A full node-by-node audit
of what changed is out of scope for this inventory regeneration; see
`docs/kernel-recon/fork-sync-2026-09/` for the DTS reconciliation work (W1-Q1 etc.).
