[
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 10,
    "topic": "file header: why this recipe is shared/consolidated",
    "category": "split",
    "replacement": "# buildroot-build — the ONE shared build recipe (do not fork per-workflow copies).\n# The incident that forced this consolidation is in docs/ci.md#why-shared-recipe.",
    "rationale": "The 'never fork this' imperative must survive at the point of use; the five-failed-runs incident narrative (ENOSPC, missing defconfig, busted toolchain caches) that proves it is reference material for docs.",
    "loadBearing": true,
    "docsSection": "why-shared-recipe"
   },
   {
    "startLine": 11,
    "endLine": 16,
    "topic": "caller vs recipe ownership boundary (part 1)",
    "category": "keep",
    "replacement": "    #\n    # The caller owns what is genuinely its own (which ref to check out, what to do\n    # with the images afterwards). Everything from \"make the runner able to build\"\n    # through \"the variant's images exist in output/images/ (main) or\n    # output-<name>/images/\" is here.\n    #",
    "rationale": "Short (4 content lines), already terse, and states the file's scope contract directly — not incident narrative, so it belongs at the point of use.",
    "loadBearing": true
   },
   {
    "startLine": 17,
    "endLine": 32,
    "topic": "measured cold-run stats + 'ccache is working' explanation",
    "category": "split",
    "replacement": "# ccache IS working as designed — before 'fixing' a small cache, read docs/ci.md#cache-budget-and-sizing.",
    "rationale": "The recurring confusion this guards against ('the cache looks tiny, is it broken?') needs a one-line stop sign; the measured GB figures and the du-rounding explanation are reference data that will go stale and belong in docs.",
    "loadBearing": true,
    "docsSection": "cache-budget-and-sizing"
   },
   {
    "startLine": 33,
    "endLine": 38,
    "topic": "warm runs smaller than cold runs, by design",
    "category": "move",
    "replacement": "# Warm runs are smaller than cold ones by design (cache #3 substitutes for ccache). See docs/ci.md#cache-budget-and-sizing.",
    "rationale": "Pure explanation with no action an editor needs to avoid; safe to relocate wholesale behind a pointer.",
    "loadBearing": false,
    "docsSection": "cache-budget-and-sizing"
   },
   {
    "startLine": 39,
    "endLine": 58,
    "topic": "VARIANTS contract: main vs kernel-only variant",
    "category": "split",
    "replacement": "# VARIANTS: \"main\" only, or a KERNEL-ONLY variant needing configs/mister_<name>.fragment —\n# targets/output-dir/cache-namespaces all derive from the name. See docs/ci.md#variants.",
    "rationale": "The load-bearing fact for someone adding a variant is 'no change needed here beyond the fragment' — that must stay; the detailed mechanics of what a kernel variant builds are reference detail.",
    "loadBearing": true,
    "docsSection": "variants"
   },
   {
    "startLine": 59,
    "endLine": 64,
    "topic": "caller vs recipe ownership boundary (part 2) + border",
    "category": "keep",
    "replacement": "    # What the CALLER does with a variant's outputs — staging the module tree into\n    # work/extra-modules-overlay/ for the main build, shipping zImage_dtb-<name> —\n    # is the caller's business (see build.yml/release.yml); this recipe ends at\n    # \"output-<name>/images/zImage_dtb and a depmod'd output-<name>/target exist\".\n    #\n    ################################################################################",
    "rationale": "Short, precise statement of where this recipe's responsibility ends — a contract boundary, not a story, so it stays inline.",
    "loadBearing": true
   },
   {
    "startLine": 110,
    "endLine": 119,
    "topic": "disk reclaim: MUST be first, ENOSPC incident, no container:",
    "category": "split",
    "replacement": "    # MUST run first — reclaims ~30GB preinstalled toolchains so output/ (24GB) fits (ENOSPC\n    # killed run 29295920820). No `container:` here — see docs/ci.md#disk-reclaim.",
    "rationale": "'MUST be first' and 'no container:' are hard constraints that stop a future edit from reordering steps or containerizing the job; the disk-math and df-logging justification is reference detail.",
    "loadBearing": true,
    "docsSection": "disk-reclaim"
   },
   {
    "startLine": 137,
    "endLine": 138,
    "topic": "BUILDROOT_VERSION source of truth",
    "category": "keep",
    "replacement": "    # BUILDROOT_VERSION lives in the Makefile (Renovate watches that same line),\n    # not in any workflow, so it cannot drift from what `make` actually fetches.",
    "rationale": "Two lines, already terse.",
    "loadBearing": false
   },
   {
    "startLine": 139,
    "endLine": 147,
    "topic": "WS_FP: toolchain not relocatable across paths",
    "category": "split",
    "replacement": "    # WS_FP keys the toolchain caches on the workspace path — cross-toolchains bake in\n    # absolute paths (NOT relocatable). See docs/ci.md#ws-fp.",
    "rationale": "The non-relocatability trap is why WS_FP exists at all and must stay; the `make sdk`/relocate-sdk.sh proof and the ccache contrast are supporting detail.",
    "loadBearing": true,
    "docsSection": "ws-fp"
   },
   {
    "startLine": 155,
    "endLine": 165,
    "topic": "variant validated once, before cache/build work",
    "category": "split",
    "replacement": "        # Variant validated ONCE, here, before any cache/build work — fragment existence IS\n        # the registry (a new variant needs no edit here). See docs/ci.md#variants.",
    "rationale": "The 'no edit needed here' fact is what stops a maintainer from adding a redundant variant allow-list elsewhere; the compute-once/consume-twice pattern explanation duplicates the BR_HOST_KEY comment below and can move.",
    "loadBearing": true,
    "docsSection": "variants"
   },
   {
    "startLine": 177,
    "endLine": 181,
    "topic": "assert kernel defconfig hasn't drifted from main",
    "category": "split",
    "replacement": "            # Assert kernel defconfig hasn't drifted from main's toolchain stanza BEFORE any\n            # cache work — a drift would build wrong under a cache key that pins it in.",
    "rationale": "Tightened from 5 to 2 lines; the warning itself IS the content, so almost nothing moves, but it no longer meets the 'already terse' bar untouched.",
    "loadBearing": true,
    "docsSection": "variants"
   },
   {
    "startLine": 197,
    "endLine": 232,
    "topic": "toolchain fingerprint: DENY-list not ALLOW-list (cortex-a9 incident)",
    "category": "split",
    "replacement": "        # DENY-list, not ALLOW-list: an allow-list once silently missed BR2_cortex_a9=y (wrong\n        # -mcpu, no cache bust). See docs/ci.md#toolchain-fingerprint before changing this filter.",
    "rationale": "The cortex-a9 silent-miscompile incident is the exact reason this must stay a deny-list; that is the trap a future 'simplify to an allow-list' refactor would hit. The exclusion-list reasoning and comment-stripping mechanics are reference detail.",
    "loadBearing": true,
    "docsSection": "toolchain-fingerprint"
   },
   {
    "startLine": 233,
    "endLine": 238,
    "topic": "which defconfig gets fingerprinted per variant",
    "category": "move",
    "replacement": "        # Fingerprints the variant's own base defconfig + fragment, not main's. See\n        # docs/ci.md#toolchain-fingerprint.",
    "rationale": "Descriptive follow-on to the deny-list warning above; no separate trap, safe to relocate.",
    "loadBearing": false,
    "docsSection": "toolchain-fingerprint"
   },
   {
    "startLine": 250,
    "endLine": 253,
    "topic": "toolchain patch dirs excluded from fingerprint",
    "category": "keep",
    "replacement": "        # Patches that rewrite the toolchain's OWN sources. patches/linux/ is\n        # deliberately not here: it changes on every kernel bump and cannot affect\n        # the cross-compiler. (None of these dirs exist today -- this closes the\n        # vector before someone opens it.)",
    "rationale": "Four lines, single topic, explains a non-obvious exclusion close to the code it governs; short enough to leave in place.",
    "loadBearing": false
   },
   {
    "startLine": 262,
    "endLine": 278,
    "topic": "grep exit 1 under pipefail is intentional",
    "category": "split",
    "replacement": "        # grep exit 1 (zero matches) is CORRECT here under pipefail — `|| [ $? -eq 1 ]`\n        # tolerates it; a real grep failure (exit >=2) still aborts. See docs/ci.md#toolchain-fingerprint.",
    "rationale": "A genuine shell trap (pipefail + intentionally-empty grep) that would confuse or break a future edit of this filter; the 'deny-list not a bug for rt today' narrative is reference detail.",
    "loadBearing": true,
    "docsSection": "toolchain-fingerprint"
   },
   {
    "startLine": 286,
    "endLine": 289,
    "topic": "sentinel assert purpose (frozen cache key is worst outcome)",
    "category": "keep",
    "replacement": "        # Fail loud if the defconfig is ever renamed/reformatted such that this\n        # produces an empty or degenerate fingerprint: a frozen cache key is the\n        # worst outcome available here (see above). Assert the two lines that no\n        # ARM defconfig can lack and that nothing legitimate ever removes.",
    "rationale": "Four lines, states the assert's purpose directly, no separable incident narrative to relocate.",
    "loadBearing": true
   },
   {
    "startLine": 299,
    "endLine": 311,
    "topic": "cache keys computed once, consumed twice",
    "category": "split",
    "replacement": "        # Cache keys computed ONCE here, consumed by restore+save below — duplicating them\n        # risks a silent 'always cold' mismatch. See docs/ci.md#cache-keys.",
    "rationale": "The compute-once/consume-twice rule is load-bearing (breaking it silently kills the cache); the hashFiles()-vs-sha256sum migration history is reference detail.",
    "loadBearing": true,
    "docsSection": "cache-keys"
   },
   {
    "startLine": 316,
    "endLine": 325,
    "topic": "variant-resolved host-toolchain key + paths, never shared",
    "category": "split",
    "replacement": "        # main's cache key stays byte-identical (back-compat); each variant gets its own\n        # br-<name>-host- namespace — NEVER share entries (absolute O= paths baked in).",
    "rationale": "The never-share-entries rule is load-bearing (sharing corrupts a toolchain); the BR_HOST_PATHS multiline/actions-cache splitting mechanics are reference detail.",
    "loadBearing": true,
    "docsSection": "cache-keys"
   },
   {
    "startLine": 350,
    "endLine": 367,
    "topic": "five input caches share GitHub's 10GB ceiling",
    "category": "split",
    "replacement": "    ############################################################################\n    # 5 input caches share GitHub's 10GB/repo LRU ceiling. Eviction is safe (self-healing\n    # cold rebuild), never wrong. Budget/levers: docs/ci.md#cache-budget-and-sizing.\n    ############################################################################",
    "rationale": "'Eviction is safe, not wrong' is the key reassurance that stops panic-fixing; the precise per-variant GB math will drift and belongs in docs.",
    "loadBearing": true,
    "docsSection": "cache-budget-and-sizing"
   },
   {
    "startLine": 369,
    "endLine": 390,
    "topic": "why dl/ and toolchain caches split restore/save differently",
    "category": "split",
    "replacement": "    # dl/ and the toolchain caches split restore/save deliberately: toolchains save\n    # GREEN-ONLY (stamps), dl/ saves via always(). See docs/ci.md#cache-save-policy.",
    "rationale": "This asymmetry is the load-bearing design fact; the actions/cache post-if internals and the run-29529993731 story are supporting detail for docs.",
    "loadBearing": true,
    "docsSection": "cache-save-policy"
   },
   {
    "startLine": 391,
    "endLine": 423,
    "topic": "dl/ and toolchain restore are COUPLED — do not decouple",
    "category": "split",
    "replacement": "    # #2 (dl/) and #3 (host toolchain) restores are COUPLED — never restore #3 without #2;\n    # its stamps lie about dl/'s contents (cost run 29534917900, 46min). See docs/ci.md#cache-coupling.",
    "rationale": "This is the flagship trap in the file: an earlier, wrong version of this exact comment cost a 46-minute build. The imperative and the incident ID must stay at the code; the pkg-generic.mk line references, failure trace, and the 'make -B source doesn't rescue this' analysis move to docs.",
    "loadBearing": true,
    "docsSection": "cache-coupling"
   },
   {
    "startLine": 425,
    "endLine": 427,
    "topic": "release tarball cache kept separate from dl/",
    "category": "keep",
    "replacement": "    # 1. Buildroot release tarball (~20-30 MB). Kept SEPARATE from dl/ on purpose:\n    # it is redundant with the copy inside dl/, but keying it on the version ALONE\n    # means a mere defconfig change can never evict this tiny, cheap entry.",
    "rationale": "Three lines, already terse.",
    "loadBearing": false
   },
   {
    "startLine": 435,
    "endLine": 443,
    "topic": "dl/ cache: location, keying, exactly-one-of-#2/#2b",
    "category": "split",
    "replacement": "    # dl/ lives at repo root (survives `make clean`) — NEVER move it under output/. Keyed\n    # on full defconfig; restore-keys falls back to version alone. See docs/ci.md#dl-cache.",
    "rationale": "'Never move it under output/' is a real constraint (it would get wiped by make clean); the 'exactly ONE of #2/#2b runs' plumbing note is implicit in the steps' own `if:` conditions and can move.",
    "loadBearing": true,
    "docsSection": "dl-cache"
   },
   {
    "startLine": 454,
    "endLine": 469,
    "topic": "variant dl/: own key, restore-keys fallback to main is safe",
    "category": "split",
    "replacement": "    # Variant dl/ gets its OWN key (cache keys are immutable, can't share). restore-keys\n    # fallback to main's dl is SAFE despite the #2/#3 coupling above. See docs/ci.md#variant-dl-fallback.",
    "rationale": "'Safe despite the coupling hazard' is the critical fact stopping someone from removing the fallback out of over-caution; the subset/superset stamp reasoning that proves it safe is the detailed justification for docs.",
    "loadBearing": true,
    "docsSection": "variant-dl-fallback"
   },
   {
    "startLine": 481,
    "endLine": 496,
    "topic": "host toolchain cache: variant-resolved path/key + coupling if:",
    "category": "split",
    "replacement": "    # `if:` implements the #2/#3 coupling — restore only when a dl restore matched, else\n    # cold build. Path/key are variant-resolved, never shared. See docs/ci.md#cache-coupling.",
    "rationale": "Ties this step's `if:` directly back to the coupling rule above, which is what a reader needs at the point where the coupling is actually enforced; the cache-matched-key mechanics are reference detail.",
    "loadBearing": true,
    "docsSection": "cache-coupling"
   },
   {
    "startLine": 505,
    "endLine": 517,
    "topic": "initramfs toolchain cache: shared across variants, coupled to dl/",
    "category": "split",
    "replacement": "    # Separate static-musl toolchain for the initramfs stage; shared across all variants\n    # (one key, one path). Coupled to dl/ same as #3 — see docs/ci.md#cache-coupling.",
    "rationale": "Shared-across-variants and the dl/-coupling are load-bearing facts; the cold/warm timing numbers and external.mk fixup mechanics are reference detail.",
    "loadBearing": true,
    "docsSection": "cache-coupling"
   },
   {
    "startLine": 528,
    "endLine": 532,
    "topic": "ccache is relocatable, not keyed on WS_FP",
    "category": "keep",
    "replacement": "    # 5. ccache. CCACHE_DIR is exported here to match the BR2_CCACHE_DIR handed to\n    # `make` below, so the action caches the exact directory Buildroot's compiler\n    # wrapper writes to. Unlike the toolchain caches this one IS relocatable —\n    # BR2_CCACHE_USE_BASEDIR rewrites the absolute paths in cached objects — so it\n    # is deliberately NOT keyed on WS_FP.",
    "rationale": "Five lines, single coherent point, no separable incident narrative — compact enough to leave as documentation for a subtle asymmetry with the toolchain caches above it.",
    "loadBearing": true
   },
   {
    "startLine": 539,
    "endLine": 540,
    "topic": "ccache-action's own apt-get needs DEBIAN_FRONTEND",
    "category": "keep",
    "replacement": "      # This action runs its own apt-get; it used to be covered by a job-level\n      # DEBIAN_FRONTEND that the callers no longer carry, so set it here.",
    "rationale": "Two lines, already terse.",
    "loadBearing": false
   },
   {
    "startLine": 544,
    "endLine": 556,
    "topic": "ccache key naming + append-timestamp is load-bearing",
    "category": "split",
    "replacement": "        # Do NOT set `append-timestamp: false` — the key is immutable, so the first save\n        # would own it forever and the hit rate would decay. See docs/ci.md#ccache-key.",
    "rationale": "Direct trap warning against a specific tempting 'fix' (turning off timestamp accumulation); the keyPrefix-stuttering naming story is reference detail.",
    "loadBearing": true,
    "docsSection": "ccache-key"
   },
   {
    "startLine": 559,
    "endLine": 563,
    "topic": "ccache-action apt index refresh, why introduced",
    "category": "keep",
    "replacement": "        # This action `apt-get install`s ccache itself, BEFORE our own apt step\n        # below has run `apt-get update` — so have it refresh the index first.\n        # (Introduced when we ran in a container with an empty index; kept because\n        # a runner's preinstalled index can be stale too, and it is what the first\n        # green run actually used.)",
    "rationale": "Five lines, single topic, no separable incident to relocate beyond what's already stated compactly.",
    "loadBearing": false
   },
   {
    "startLine": 566,
    "endLine": 573,
    "topic": "apt package list rationale, one line per package",
    "category": "move",
    "replacement": "    # Package list = Buildroot's mandatory set + repo extras; per-package rationale in\n    # docs/ci.md#apt-deps.",
    "rationale": "A reference table (which package needs which thing) with no trap for an editor beyond 'don't remove one without checking' — safe to relocate to a docs table.",
    "loadBearing": false,
    "docsSection": "apt-deps"
   },
   {
    "startLine": 574,
    "endLine": 577,
    "topic": "EXTRA_APT via env avoids script-injection footgun",
    "category": "split",
    "replacement": "    # EXTRA_APT goes through env, not `${{ }}` interpolation — avoids the Actions\n    # script-injection footgun.",
    "rationale": "Security-relevant trap warning must stay inline; the 'value is ours today but it's cheap' hedge is filler that can be dropped in the move.",
    "loadBearing": true,
    "docsSection": "apt-deps"
   },
   {
    "startLine": 586,
    "endLine": 586,
    "topic": "shellcheck disable directive for EXTRA_APT splitting",
    "category": "keep",
    "replacement": "        # shellcheck disable=SC2086 -- word splitting of EXTRA_APT is intended",
    "rationale": "One line, a functional shellcheck directive tied to the code immediately below it.",
    "loadBearing": false
   },
   {
    "startLine": 597,
    "endLine": 599,
    "topic": "verify GNU coreutils `install` before the long build",
    "category": "keep",
    "replacement": "    # Fails fast and loud if the runner's `install` is ever not GNU coreutils\n    # (see the Makefile's `hostshim` target) — cheap to check before spending\n    # three hours of build time.",
    "rationale": "Three lines, already terse.",
    "loadBearing": false
   },
   {
    "startLine": 604,
    "endLine": 616,
    "topic": "regenerate .config unconditionally (stale-config incident)",
    "category": "split",
    "replacement": "    # Regenerate output/.config UNCONDITIONALLY every run — a stale cached one can\n    # silently override a defconfig change (run 29293209070 died here after a 52min stage 1).",
    "rationale": "The incident and the 'why unconditional' imperative must stay; the MAIN-ONLY explanation of why variants can't hit this staleness (their .config is never cached) is reference detail, and is also implied by the step's own `if:`.",
    "loadBearing": true,
    "docsSection": "configure-buildroot"
   },
   {
    "startLine": 622,
    "endLine": 631,
    "topic": "make all builds both stages; MISTER_VERSION passthrough",
    "category": "move",
    "replacement": "    # `make all` builds both stages under one ccache; MISTER_VERSION passthrough is\n    # release.yml's (ADR 0018). See docs/ci.md#build-step.",
    "rationale": "Pure description of what the step does and why an unrelated env var is inherited — no trap for a future editor, safe to relocate.",
    "loadBearing": false,
    "docsSection": "build-step"
   },
   {
    "startLine": 641,
    "endLine": 650,
    "topic": "kernel variant build: Makefile hard-asserts config, fails in CI not on device",
    "category": "split",
    "replacement": "    # Makefile's variant target hard-asserts the built kernel's config (e.g.\n    # CONFIG_PREEMPT_RT=y) — a fragment regression fails HERE, loudly, not on a device.",
    "rationale": "The safety property (fail in CI rather than in the field) is the load-bearing fact; the mechanical description of what `make <name>` does is reference detail.",
    "loadBearing": true,
    "docsSection": "build-step"
   },
   {
    "startLine": 660,
    "endLine": 663,
    "topic": "BR_LEGAL_TARGET is variant-resolved",
    "category": "keep",
    "replacement": "    # BR_LEGAL_TARGET is variant-resolved in the fingerprint step: `legal-info`\n    # for main, `<name>-legal-info` for a kernel variant (same recipe, aimed at\n    # output-<name>/ — kernel-only, so the bundle is dominated by the kernel\n    # source).",
    "rationale": "Four lines, already terse, no incident to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 673,
    "endLine": 680,
    "topic": "save section banner: pass-or-fail, placed after legal-info",
    "category": "split",
    "replacement": "    ############################################################################\n    # Save the download caches — PASS OR FAIL (dl/'s stamp-coupling reasoning:\n    # docs/ci.md#cache-coupling). Placed after legal-info, which itself may fetch\n    # tarballs missing from dl/.\n    ############################################################################",
    "rationale": "Its original pointer ('see the long comment above the restore steps') goes stale once that comment's body moves to docs — the pointer needs to target docs/ci.md directly instead of a paragraph that will no longer be there.",
    "loadBearing": false,
    "docsSection": "cache-coupling"
   },
   {
    "startLine": 682,
    "endLine": 695,
    "topic": "TRAP: partial dl/ save locks a stub into the immutable key",
    "category": "split",
    "replacement": "        # TRAP: a partial dl/ save LOCKS a stub into the immutable cache key forever (exact\n        # hits skip the repair save). Verify completeness before saving — docs/ci.md#dl-completeness.",
    "rationale": "The lock-in trap is the load-bearing warning; the 'a late failure is fine, run 29529993731 proves it' reasoning is supporting evidence for docs.",
    "loadBearing": true,
    "docsSection": "dl-completeness"
   },
   {
    "startLine": 696,
    "endLine": 701,
    "topic": "do not use a size floor for dl/ completeness (incident)",
    "category": "split",
    "replacement": "        # Do NOT use a size floor for dl/ completeness — a 2GB floor passed run 29534917900\n        # while missing the exact file legal-info needed. See docs/ci.md#dl-completeness.",
    "rationale": "A 'we tried the obvious thing, it failed' trap — critical to stop someone reintroducing a size-floor heuristic; the run number is the proof and must stay.",
    "loadBearing": true,
    "docsSection": "dl-completeness"
   },
   {
    "startLine": 702,
    "endLine": 720,
    "topic": "external-deps oracle must use same BR2_CCACHE=y flag (158 vs 163 files)",
    "category": "split",
    "replacement": "        # MUST pass the same BR2_CCACHE=y as the build — omitting it undercounts dl/'s\n        # needs by 5 files (158 vs 163, misses ccache itself). See docs/ci.md#dl-completeness.",
    "rationale": "The exact-flag-must-match trap, with verified file counts, is what stops a future 'simplify this invocation' edit from silently breaking completeness checking; the oracle's -B/stamp-blindness mechanics are reference detail.",
    "loadBearing": true,
    "docsSection": "dl-completeness"
   },
   {
    "startLine": 728,
    "endLine": 736,
    "topic": "basename filter regex assumptions",
    "category": "split",
    "replacement": "        # Filter assumes no legitimate dl/ basename contains '/', '=', space, or starts with\n        # '#' — extra Makefile/kconfig stdout noise relies on that. See docs/ci.md#dl-completeness.",
    "rationale": "This is the constraint a future change to the wrapper Makefile's echo output could silently violate; the itemized list of exactly which stdout lines trigger the guard is reference detail.",
    "loadBearing": true,
    "docsSection": "dl-completeness"
   },
   {
    "startLine": 761,
    "endLine": 764,
    "topic": "tarball existence check done in shell, not hashFiles()",
    "category": "keep",
    "replacement": "        # Existence check for the tarball save below, done here in shell rather\n        # than as hashFiles() in that step's `if:` — one less expression-context\n        # subtlety to be wrong about, and `actions/cache/save` hard-errors on a\n        # missing path rather than skipping.",
    "rationale": "Four lines, explains a subtle but self-contained design choice; no incident to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 771,
    "endLine": 782,
    "topic": "dl/ save: cache-hit semantics and variant key-namespace isolation",
    "category": "split",
    "replacement": "    # `||` in `key:` resolves to whichever dl restore ran, keeping a variant's save inside\n    # its own key namespace — never writes into main's. See docs/ci.md#dl-cache-save.",
    "rationale": "The never-cross-namespaces guarantee is load-bearing (violating it would corrupt main's cache); the exact-vs-restore-keys cache-hit semantics are reference detail.",
    "loadBearing": true,
    "docsSection": "dl-cache-save"
   },
   {
    "startLine": 797,
    "endLine": 815,
    "topic": "toolchain saves are GREEN-ONLY; absence of always() is deliberate",
    "category": "split",
    "replacement": "    ############################################################################\n    # Save the toolchain caches — GREEN BUILDS ONLY. No `always()` here on purpose\n    # (implicit success() gate); a half-built output/host WITH its stamps is a\n    # poisoned toolchain. See docs/ci.md#toolchain-save-policy.\n    ############################################################################",
    "rationale": "The original comment calls this out as 'the one place in this file where the absence of always() is load-bearing rather than an oversight' — exactly the trap that must stop a future editor from 'fixing' a perceived inconsistency with the always() used elsewhere.",
    "loadBearing": true,
    "docsSection": "toolchain-save-policy"
   },
   {
    "startLine": 823,
    "endLine": 826,
    "topic": "initramfs save: every variant saves it, key is variant-independent",
    "category": "keep",
    "replacement": "    # Every variant, like its restore: `make <variant>` builds the initramfs\n    # stage too (its cpio is embedded into every kernel), so output-initramfs/\n    # exists on green variant runs as well — and the key is variant-independent,\n    # so whichever variant builds it cold saves it for all.",
    "rationale": "Four lines, already terse, no separable incident.",
    "loadBearing": false
   }
  ],
  "file": ".github/actions/buildroot-build/action.yml",
  "totals": {
   "keep": 14,
   "move": 4,
   "split": 29,
   "drop": 0,
   "commentLinesBefore": 494,
   "commentLinesAfter": 114
  },
  "proposedDocSections": [
   "why-shared-recipe",
   "cache-budget-and-sizing",
   "variants",
   "toolchain-fingerprint",
   "cache-keys",
   "cache-save-policy",
   "cache-coupling",
   "dl-cache",
   "variant-dl-fallback",
   "disk-reclaim",
   "ws-fp",
   "ccache-key",
   "apt-deps",
   "configure-buildroot",
   "build-step",
   "dl-completeness",
   "dl-cache-save",
   "toolchain-save-policy"
  ]
 },
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 4,
    "topic": "File title/purpose banner",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "################################################################################\n#\n# release.yml — tag-triggered release workflow + build provenance (TASKS.md\n# P4.4, PLAN.md §9 \"Build\", docs/downloader-contract.md P0.6).",
    "rationale": "Already a terse identifier plus task-brief pointers; nothing here needs relocating."
   },
   {
    "startLine": 5,
    "endLine": 31,
    "topic": "Three downstream consumers of release assets (human, Downloader, RT beta)",
    "category": "split",
    "docsSection": "Release consumers (three downstream actors)",
    "loadBearing": true,
    "replacement": "#\n# Rebuilds via the SAME recipe as build.yml, then packages a GitHub Release for\n# 3 consumers: a human, the on-device Downloader_MiSTer (never talks to GitHub\n# Releases directly), and the opt-in RT beta kernel. See docs/ci.md#release-consumers.",
    "rationale": "The full breakdown of what each consumer needs (esp. why actor #2's contract is unforgiving) is design documentation a maintainer reads once, not a warning that must survive at the code; a 4-line summary naming the three actors plus a pointer preserves the map without the essay."
   },
   {
    "startLine": 32,
    "endLine": 40,
    "topic": "Why release.yml rebuilds instead of depending on build.yml's run",
    "category": "split",
    "docsSection": "Why release.yml rebuilds and re-verifies instead of adopting build.yml's run",
    "loadBearing": true,
    "replacement": "#\n# Rebuilds from the tagged commit rather than adopting build.yml's run/artifacts\n# — provenance must attest a build of THIS commit. See docs/ci.md#rebuild-not-adopt.",
    "rationale": "Names a real past incident: the five fixes that got build.yml green never reached this file, so the first real tag push would have failed here. The rule (never adopt another run's artifacts for a release) must stay as a warning; the incident narrative moves."
   },
   {
    "startLine": 41,
    "endLine": 48,
    "topic": "Why release.yml re-verifies (ci-tests+check-abi) rather than trusting build.yml",
    "category": "split",
    "docsSection": "Why release.yml rebuilds and re-verifies instead of adopting build.yml's run",
    "loadBearing": false,
    "replacement": "#\n# Re-verifies too (ci-tests.sh + check-abi.sh), not just re-builds — running\n# only one of that pair would be an incoherent middle. See docs/ci.md#rebuild-not-adopt.",
    "rationale": "Direct continuation of the rebuild argument applied to verification; consolidate under one doc section instead of stating the same reasoning twice in the header."
   },
   {
    "startLine": 49,
    "endLine": 66,
    "topic": "Why the stock uboot.img/files/linux/ payload is fetched, not vendored (G6) + its pins",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": true,
    "replacement": "#\n# Fetches the stock uboot.img/files/linux/ payload at release time (G6: no\n# binaries in git), hash-pinned to a specific upstream commit/archive.\n# See docs/ci.md#stock-payload-sourcing.",
    "rationale": "The G6 rationale and the exact hash values are restated verbatim in the env block below (lines 301-314) — keep one pointer here and let the env block (the actual operative pins) plus docs carry the detail, so the numbers can't drift out of sync between prose and code."
   },
   {
    "startLine": 67,
    "endLine": 75,
    "topic": "TODO: open human decision on third-party stock-payload dependency",
    "category": "split",
    "docsSection": "Open decision: third-party stock-payload dependency",
    "loadBearing": true,
    "replacement": "#\n# TODO(human): confirm the third-party stock-payload sourcing choice before the\n# first real tag push — not yet ratified. See docs/ci.md#stock-payload-open-decision.",
    "rationale": "A live decision a human still owes a sanity check on; keep it visible as a one-line TODO so it isn't silently forgotten, move the full reasoning (mt32-rom-data/soundfonts provenance, third-party git blob dependency) to docs."
   },
   {
    "startLine": 76,
    "endLine": 89,
    "topic": "Tag convention (unratified) + MiSTer.version/release-filename/db.json decoupling from the git tag",
    "category": "split",
    "docsSection": "MiSTer.version / release filename / db.json coupling (load-bearing)",
    "loadBearing": true,
    "replacement": "#\n# TAG CONVENTION: v* is unratified — see docs/ci.md#tag-convention.\n#\n# TRAP: release version is DELIBERATELY DECOUPLED from the git tag (driven by\n# /MiSTer.version, ADR 0018) — don't \"fix\" this coupling. See docs/ci.md#mister-version-coupling.",
    "rationale": "This is the file's sharpest trap and is named explicitly in the task brief: a maintainer 'simplifying' the version to just use the git tag would silently break the Downloader's update-offer logic. The warning must stay at the code; the full explanation (why not SOURCE_DATE_EPOCH, what breaks) moves."
   },
   {
    "startLine": 90,
    "endLine": 91,
    "topic": "Closing header rule",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "#\n################################################################################",
    "rationale": "Pure formatting."
   },
   {
    "startLine": 99,
    "endLine": 105,
    "topic": "workflow_dispatch exists only for the opt-in full sdcard variant",
    "category": "split",
    "docsSection": "Manual workflow_dispatch: opt-in full sdcard variant",
    "loadBearing": true,
    "replacement": "  # Manual runs exist ONLY to build the opt-in full sdcard variant — dispatch\n  # AGAINST A TAG, or `publish` never attaches the assets to a release.\n  # See docs/ci.md#manual-full-sdcard-dispatch.",
    "rationale": "The operational rule (dispatch against a tag, not a branch) is what a human triggering this by hand needs to not get silently no-op'd; the ref_name mechanics explanation moves to docs."
   },
   {
    "startLine": 113,
    "endLine": 115,
    "topic": "Concurrency group rationale",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "# A re-pushed (corrected) tag should supersede an in-flight run for the same\n# tag; distinct tags already get distinct concurrency groups via github.ref,\n# so this never cancels a run for a DIFFERENT release.",
    "rationale": "Already terse (3 lines), single point, stays as-is."
   },
   {
    "startLine": 120,
    "endLine": 122,
    "topic": "Least-privilege permissions rationale",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "# Least privilege at the workflow level; the `publish` job elevates its own\n# permissions block (contents: write, id-token: write, attestations: write)\n# only for the steps that actually need them (attest + create the release).",
    "rationale": "Already terse (3 lines), stays as-is."
   },
   {
    "startLine": 127,
    "endLine": 135,
    "topic": "build-kernel job: matrix, per-leg artifact contents, GPL-bundle vs SBOM",
    "category": "split",
    "docsSection": "Kernel-variant matrix (build-kernel) design",
    "loadBearing": false,
    "replacement": "  # ============================================================================\n  # build-kernel — the kernel-variant matrix (ADR 0021 as amended 2026-07-18).\n  # Ships full legal-info (GPL source), not a manifest-only SBOM — a release\n  # DISTRIBUTES the binary. See docs/ci.md#kernel-variant-matrix.",
    "rationale": "The GPL-source-bundle-vs-SBOM distinction is useful design reasoning but not something a silent future edit could break unnoticed; a terse summary plus doc pointer covers the code site."
   },
   {
    "startLine": 136,
    "endLine": 139,
    "topic": "No MISTER_VERSION derivation in build-kernel (intentional absence)",
    "category": "keep",
    "docsSection": "",
    "loadBearing": true,
    "replacement": "  #\n  # No MISTER_VERSION derivation here: nothing in a kernel-only build bakes it\n  # (there is no rootfs to write /MiSTer.version into) — the duplicate step the\n  # old two-image design carried is gone, not forgotten.",
    "rationale": "Explains an intentional absence in 4 lines — exactly the sort of note that must stay AT the code so nobody 'fixes' the apparent gap by re-adding a redundant derivation step."
   },
   {
    "startLine": 179,
    "endLine": 197,
    "topic": "Adding a kernel variant + no-container pointer",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "  #\n  # Adding a variant = one configs/mister_<name>.fragment (plus its Makefile\n  # targets) — nothing to edit in THIS matrix. It's `fromJSON` of the\n  # `kernel-variants` job's output, which `scripts/list-kernel-variants.sh`\n  # derives from the filesystem (configs/mister_*.fragment is the registry —\n  # the same one .github/actions/buildroot-build's fragment-existence check\n  # reads), so build.yml and this file consume the identical, single-source\n  # list instead of each hand-maintaining its own matrix literal.\n  #\n  # Downstream in `publish`, the release asset list and the provenance\n  # attestation `subject-path` are ALSO derived (globbed off dist/, not\n  # named), so those need no edit either. What still DOES need a hand-edit\n  # for a new variant: the release-notes prose in `publish` (human-readable\n  # description of what the variant is) and `scripts/mk-sdcard.sh`'s single\n  # hardcoded bonus-kernel slot (one card, one bonus kernel, by that script's\n  # own design — not a variant list). See docs/rt-beta-kernel.md's \"Adding a\n  # future kernel variant\" paragraph for the full accounting.\n  # NO `container:` — see .github/actions/buildroot-build (same as build).\n  # ============================================================================",
    "rationale": "Stale after item D: item D inserted the new `kernel-variants` job (~35 lines) above this block, shifting its old 140-143 anchor into the middle of that job's rationale comment, and item D's fix (this review pass) also rewrote the block's own content -- the old 'one matrix entry' sentence this replacement carried was the exact false claim item D exists to correct, and the block now also documents that publish's asset list/attestation are derived while the release notes and mk-sdcard.sh's bonus-kernel slot are not. Re-verify against the file before item H applies this triage; category left as 'keep' (still just a design-rationale comment, not load-bearing) but the growth here is a genuine 'is this still terse enough' question item H should re-decide, not an editorial choice frozen by this refresh."
   },
   {
    "startLine": 151,
    "endLine": 153,
    "topic": "build-kernel timeout estimate, cross-referenced to build.yml",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "    # Same honestly-unmeasured estimate as build.yml's build-kernel: cold =\n    # cross-toolchain (~1h) + stage-1 initramfs (~30 min) + one kernel build;\n    # 240 is ~2x that. Re-derive from real runs once they exist.",
    "rationale": "Already a terse cross-file pointer with a unique local number; stays as-is."
   },
   {
    "startLine": 166,
    "endLine": 168,
    "topic": "Kernel build overview step, mirrors build.yml",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Best-effort overview, mirroring build.yml's kernel leg: every fact\n      # guarded, a missing one never fails the build. Uniqueness-guarded\n      # linux-[0-9]* glob for the same reasons documented there.",
    "rationale": "Already terse (3 lines), pointer-style, stays as-is."
   },
   {
    "startLine": 202,
    "endLine": 204,
    "topic": "Kernel artifact staging, mirrors build.yml",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Same uniqueness-guarded staging as build.yml's leg (see the comments\n      # there), plus the FULL legal-info bundle with the same 2 GiB per-asset\n      # guard the main legal-info step documents.",
    "rationale": "Already terse (3 lines), pointer-style, stays as-is."
   },
   {
    "startLine": 210,
    "endLine": 211,
    "topic": "nullglob rationale in staging script",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # nullglob so a zero-match glob yields an EMPTY array, keeping the\n          # counts in the guard errors truthful — see build.yml's staging step.",
    "rationale": "Already terse (2 lines), stays as-is."
   },
   {
    "startLine": 248,
    "endLine": 254,
    "topic": "Kernel artifact upload: sha-named, the '/' in ref_name trap",
    "category": "split",
    "docsSection": "Artifact naming: sha vs ref_name ('/' trap)",
    "loadBearing": true,
    "replacement": "      # sha-named, not ref_name — upload-artifact REJECTS '/' in names, and every\n      # branch here is slash-named. See docs/ci.md#artifact-naming-slash-trap.",
    "rationale": "This is the fullest statement of the trap in the file (build.yml's own equivalent upload step carries NO comment at all). Keep the warning at each site that needs it, but move the one full explanation to docs so it's written once instead of drifting across the several places it recurs."
   },
   {
    "startLine": 263,
    "endLine": 268,
    "topic": "build job: what it produces and when it runs",
    "category": "split",
    "docsSection": "release build job overview",
    "loadBearing": false,
    "replacement": "  # ============================================================================\n  # build — produces every release asset + the pinned-7za-verified archive; runs\n  # after build-kernel to merge leg module trees. See docs/ci.md#release-build-job.",
    "rationale": "Structural summary of the job's role, already largely evident from `needs:` and step names; a short pointer preserves the orientation without restating the DAG in prose."
   },
   {
    "startLine": 269,
    "endLine": 272,
    "topic": "NO container: disk-reclaim requirement",
    "category": "split",
    "docsSection": "No container / disk reclaim (shared with reproducibility.yml)",
    "loadBearing": true,
    "replacement": "  #\n  # NO `container:` — needs host-level disk reclaim to fit a Buildroot build.\n  # See docs/ci.md#no-container-disk-reclaim.\n  # ============================================================================",
    "rationale": "reproducibility.yml carries near-identical reasoning in its own words (lines 107-112 there) — this becomes ONE doc section both files point at instead of two independently-drifting explanations."
   },
   {
    "startLine": 278,
    "endLine": 292,
    "topic": "355-min timeout: sdcard-step reuse and the 6h runner-cap incident",
    "category": "split",
    "docsSection": "sdcard build timing and the 6-hour runner cap",
    "loadBearing": true,
    "replacement": "    # 355 min: sdcard steps REUSE output/ (relink only, ~15 min each way) — do\n    # NOT reintroduce a from-scratch sdcard build; an earlier design that did\n    # overran GitHub's 360-min cap and the release never published.\n    # See docs/ci.md#sdcard-timing-6h-cap.",
    "rationale": "Names a real incident (an earlier from-scratch-per-sdcard-step design overran the 360-min hard cap and the release never published) — exactly the kind of trap the task flags for splitting. The warning against reintroducing it stays; the timing derivation moves."
   },
   {
    "startLine": 301,
    "endLine": 306,
    "topic": "Pinned stock reference archive: doc pointers + G6 restated",
    "category": "move",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": false,
    "replacement": "      # Pinned stock reference archive (G6: no binaries in git). See docs/ci.md#stock-payload-sourcing.",
    "rationale": "Restates the header's G6/cannot-vendor rationale right next to the actual pinned values below it. The values (URL/MD5/SHA256/size, which stay untouched) are the load-bearing part; the prose duplicates the header and can be a pure pointer."
   },
   {
    "startLine": 316,
    "endLine": 324,
    "topic": "Pinned ARM 7za: the URL is an unpinned floating ref by design",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": true,
    "replacement": "      # --- Pinned ARM static 7za --------------------------------------------\n      # URL is a floating branch ref BY DESIGN (matches the real Downloader's own\n      # unpinned source) — the MD5 check below is the actual security control.\n      # See docs/ci.md#stock-payload-sourcing.",
    "rationale": "A future maintainer could reasonably 'fix' this into a commit pin, which would break parity with what the real on-device Downloader actually fetches — the warning must stay, the constants.py backstory moves."
   },
   {
    "startLine": 333,
    "endLine": 339,
    "topic": "Derive release version step: must run before the build",
    "category": "split",
    "docsSection": "MiSTer.version / release filename / db.json coupling (load-bearing)",
    "loadBearing": true,
    "replacement": "      # Must run BEFORE the build — post-build.sh bakes MISTER_VERSION into\n      # /MiSTer.version (ADR 0018). See docs/ci.md#mister-version-coupling.",
    "rationale": "Part of the version-coupling trap family named in the task brief; consolidate its reasoning under the same doc section as the header's version paragraph instead of re-explaining it here."
   },
   {
    "startLine": 351,
    "endLine": 352,
    "topic": "Kernel-leg artifacts download step summary",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # The kernel legs' artifacts: module trees for the overlay (next step),\n      # kernel/config/legal-info files staged into dist/ further down.",
    "rationale": "Already terse (2 lines), stays as-is."
   },
   {
    "startLine": 360,
    "endLine": 362,
    "topic": "Extra-modules overlay population, mirrors build.yml",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Same mechanism, same comments as build.yml's step of the same name: the\n      # main defconfig's BR2_ROOTFS_OVERLAY folds this directory into the one\n      # shipped linux.img at target-finalize.",
    "rationale": "Already terse (3 lines), pointer-style, stays as-is."
   },
   {
    "startLine": 380,
    "endLine": 386,
    "topic": "Build image step: two different 7za binaries, don't conflate them",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": true,
    "replacement": "      # NOTE: this p7zip-full 7za (host, creates the archive) is NOT the pinned\n      # ARM 7za verified below (proves on-device compat) — don't conflate them.\n      # See docs/ci.md#stock-payload-sourcing.",
    "rationale": "The distinction between the host tool that assembles the archive and the pinned ARM binary that proves on-device compatibility is the part someone could get confused by; the apt-package mechanics move."
   },
   {
    "startLine": 390,
    "endLine": 397,
    "topic": "extra-apt-packages: purpose of each package",
    "category": "move",
    "docsSection": "Release job apt packages (host-only tools)",
    "loadBearing": false,
    "replacement": "          # Host-only build tools for release assembly + mk-sdcard.sh. See docs/ci.md#release-apt-packages.",
    "rationale": "Pure reference documentation (which package does what) with no trap or incident attached — safe to relocate wholesale behind a pointer."
   },
   {
    "startLine": 401,
    "endLine": 402,
    "topic": "Module-tree merge assertion, mirrors build.yml",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Same silent-overlay-miss guard as build.yml: a variant kernel shipped\n      # without its modules in linux.img must fail HERE, not on a device.",
    "rationale": "Already terse (2 lines), stays as-is."
   },
   {
    "startLine": 418,
    "endLine": 426,
    "topic": "Parity test suite: re-run here rather than trust build.yml",
    "category": "split",
    "docsSection": "Why release.yml rebuilds and re-verifies instead of adopting build.yml's run",
    "loadBearing": false,
    "replacement": "      # Re-runs ci-tests.sh + check-abi.sh against THIS build (a tag may point at\n      # a commit build.yml never validated) — same pair, same flags as build.yml.\n      # See docs/ci.md#rebuild-not-adopt.",
    "rationale": "Restates the header's rebuild/re-verify rationale a second time; consolidate to the one doc section instead of two near-identical explanations."
   },
   {
    "startLine": 432,
    "endLine": 434,
    "topic": "Upload parity-suite results, if: always()",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Worth having precisely when the step above FAILED -- if: always() so a\n      # failed release build still surfaces the machine-readable failure digest\n      # (mirrors build.yml). ~2 KB; negligible.",
    "rationale": "Already terse (3 lines), stays as-is."
   },
   {
    "startLine": 439,
    "endLine": 441,
    "topic": "sha-named parity-results upload, pointer to naming trap",
    "category": "move",
    "docsSection": "Artifact naming: sha vs ref_name ('/' trap)",
    "loadBearing": false,
    "replacement": "          # sha-named — a branch ref's '/' breaks upload-artifact. See docs/ci.md#artifact-naming-slash-trap.",
    "rationale": "Already a short in-file cross-reference; repoint it at the consolidated docs/ci.md section instead of 'see the kernel artifact's naming comment' so it survives future reordering of the file."
   },
   {
    "startLine": 447,
    "endLine": 452,
    "topic": "ABI/SONAME checker: stock-binary gates SKIP in this clean checkout",
    "category": "split",
    "docsSection": "ABI/SONAME check behavior in clean checkout",
    "loadBearing": true,
    "replacement": "      # check-abi.sh against output/target; the stock-binary gates (A-10/A-22)\n      # SKIP in this clean checkout (no work/). See docs/ci.md#release-abi-check.",
    "rationale": "The non-obvious behavior (some gates silently skip here because work/ is gitignored) is exactly what a future reader needs to not mistake for a broken check; keep it as a warning, move the rest."
   },
   {
    "startLine": 456,
    "endLine": 466,
    "topic": "dist/ assembly: the 7-asset contract plus variant/sdcard extras",
    "category": "split",
    "docsSection": "dist/ layout contract",
    "loadBearing": false,
    "replacement": "      ##########################################################################\n      # dist/ = the 7 Downloader-contract assets (TASKS.md P4.4) + per-variant\n      # extras (ADR 0021) + the sdcard images below; `publish` uploads it verbatim.\n      # See docs/ci.md#dist-layout.\n      ##########################################################################",
    "rationale": "Functions as a section banner/table-of-contents for the steps that follow; a short orienting line plus pointer keeps that function without the full enumeration."
   },
   {
    "startLine": 486,
    "endLine": 503,
    "topic": "legal-info.tar.gz: host-sources exclusion + the 2109 MiB near-miss",
    "category": "split",
    "docsSection": "legal-info.tar.gz: host-sources exclusion and the 2 GiB asset cap",
    "loadBearing": true,
    "replacement": "      # host-sources/ MUST stay excluded — with it the archive hit 2109 MiB, over\n      # GitHub's 2 GiB per-asset cap; the first real tag push would have failed\n      # right at upload. See docs/ci.md#legal-info-2gib-cap.",
    "rationale": "Names a real measured incident (2109 MiB, would have failed at the API) — the warning against re-including host-sources/ must survive at the code; the GPL-obligation legal reasoning moves."
   },
   {
    "startLine": 513,
    "endLine": 514,
    "topic": "2 GiB per-asset ceiling, inline guard comment",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # GitHub's hard per-asset ceiling is 2 GiB. Fail here, with a clear\n          # message, rather than at the upload.",
    "rationale": "Already terse (2 lines), sits directly above the enforcing code, stays as-is."
   },
   {
    "startLine": 523,
    "endLine": 527,
    "topic": "Kernel-variant asset staging: glob-must-match trap",
    "category": "split",
    "docsSection": "dist/ layout contract",
    "loadBearing": true,
    "replacement": "      # Copied by glob (future matrix entries need no edit here); a zero-match\n      # glob silently changes shape — cp's own failure catches that.",
    "rationale": "The failure mode (a zero-match glob silently shrinking the staged set, caught only by cp's own error) is the actionable warning; the 'legs already applied guards' framing moves."
   },
   {
    "startLine": 535,
    "endLine": 557,
    "topic": "LOAD-BEARING: /MiSTer.version -> RELEASE_DATE, the core of the version-coupling trap",
    "category": "split",
    "docsSection": "MiSTer.version / release filename / db.json coupling (load-bearing)",
    "loadBearing": true,
    "replacement": "      ##########################################################################\n      # LOAD-BEARING: release_YYYYMMDD.7z / SHA256SUMS / db.json version must all\n      # equal the /MiSTer.version this job just baked (ADR 0018) — do not decouple.\n      # See docs/ci.md#mister-version-coupling.\n      ##########################################################################",
    "rationale": "THE central trap of the file, explicitly named in the task brief. The sharp rule (three values must all agree, byte-for-byte) stays inline; the full narrative (the old constant-version hazard this fixed, why not SOURCE_DATE_EPOCH) moves to the one doc section shared with the header and the derive-version step."
   },
   {
    "startLine": 588,
    "endLine": 589,
    "topic": "Verify MISTER_VERSION override honored",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # Verify the build honored the MISTER_VERSION override (ADR 0018):\n          # /MiSTer.version must equal what we asked post-build.sh to bake.",
    "rationale": "Already terse (2 lines), stays as-is."
   },
   {
    "startLine": 594,
    "endLine": 596,
    "topic": "Flagged assumption: hardcoded century \"20\"",
    "category": "keep",
    "docsSection": "",
    "loadBearing": true,
    "replacement": "          # Assumption, flagged: century is hardcoded \"20\" (valid until the\n          # year 2100). /MiSTer.version itself only ever carries YYMMDD, same\n          # as stock's own convention (e.g. \"250402\" for release_20250402.7z).",
    "rationale": "A flagged long-lived assumption in 3 lines, exactly where a maintainer touching this code would need to see it; stays as-is."
   },
   {
    "startLine": 601,
    "endLine": 607,
    "topic": "Stock archive verification: must fully pass before extraction",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": true,
    "replacement": "      ##########################################################################\n      # MUST fully verify (MD5+size+7z CRC) BEFORE extracting a byte — mirrors\n      # the real Downloader's own ordering. See docs/ci.md#stock-payload-sourcing.\n      ##########################################################################",
    "rationale": "The ordering requirement (verify everything, twice, before extracting) is the load-bearing part; the doc-section citation detail moves to the consolidated section."
   },
   {
    "startLine": 631,
    "endLine": 632,
    "topic": "Second independent 7z-CRC check, mirrors Downloader",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # Second, independent check: 7z's own internal CRCs, same as the\n          # real Downloader's `7za t` step (docs/downloader-contract.md §5).",
    "rationale": "Already terse (2 lines), stays as-is."
   },
   {
    "startLine": 644,
    "endLine": 650,
    "topic": "Extraction pattern matches the real Downloader's",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": false,
    "replacement": "          # Exactly the Downloader's own extraction pattern — Windows-installer-\n          # only members are never shipped. See docs/ci.md#stock-payload-sourcing.",
    "rationale": "Reference detail (which stock files are never extracted, and why) belongs in the consolidated payload-sourcing doc section rather than repeated at each extraction step."
   },
   {
    "startLine": 669,
    "endLine": 676,
    "topic": "Assemble release tree: overlay semantics mirror the on-device rsync",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": false,
    "replacement": "      ##########################################################################\n      # Overlay: verified stock files/linux/ + our linux.img/zImage_dtb — mirrors\n      # the Downloader's on-device rsync. See docs/ci.md#stock-payload-sourcing.\n      ##########################################################################",
    "rationale": "Same theme as the other stock-payload steps; short pointer keeps the 'why this ordering matches the device' link without repeating it a fourth time."
   },
   {
    "startLine": 692,
    "endLine": 696,
    "topic": "Plain LZMA2, not stock's BCJ2 filter",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": false,
    "replacement": "          # Plain solid LZMA2, not stock's BCJ2 (irrelevant Windows-7-Zip-GUI\n          # artifact). See docs/ci.md#stock-payload-sourcing.",
    "rationale": "Explains a deliberate deviation from stock's archive format; a short pointer plus the existing doc citation is enough."
   },
   {
    "startLine": 700,
    "endLine": 705,
    "topic": "Verify with the pinned ARM 7za, not host 7z",
    "category": "split",
    "docsSection": "Stock payload sourcing & verification order",
    "loadBearing": true,
    "replacement": "      ##########################################################################\n      # Verify with the EXACT pinned ARM 7za, not a modern host 7z — testing\n      # anything less specific doesn't prove the constraint. See docs/ci.md#stock-payload-sourcing.\n      ##########################################################################",
    "rationale": "The rule (must use the exact pinned ARM binary, not a convenient host substitute) is the actionable part a 'simplification' could weaken."
   },
   {
    "startLine": 724,
    "endLine": 724,
    "topic": "Integrity test = Downloader's first invocation",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # 1. Integrity test -- exactly the Downloader's first 7za invocation.",
    "rationale": "Single line, stays as-is."
   },
   {
    "startLine": 726,
    "endLine": 728,
    "topic": "Extraction = Downloader's second invocation",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # 2. Extraction -- exactly the Downloader's second invocation\n          #    (docs/downloader-contract.md §5): files/linux/* only, into a\n          #    fresh directory.",
    "rationale": "Already terse (3 lines), stays as-is."
   },
   {
    "startLine": 751,
    "endLine": 755,
    "topic": "7z l -slt output parsing: skip the archive's own Path line",
    "category": "split",
    "docsSection": "7z l -slt output parsing trap",
    "loadBearing": true,
    "replacement": "          # `7z l -slt` prints the ARCHIVE's own Path line before the member list\n          # — skip past the \"----------\" separator or it corrupts this diff.",
    "rationale": "A genuine parsing gotcha in a specific tool's output format — the exact kind of trap a future refactor of this pipeline could reintroduce if not warned inline."
   },
   {
    "startLine": 765,
    "endLine": 768,
    "topic": "Explicit cross-check against the documented canonical member set",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "          # Explicit, human-readable cross-check against the documented\n          # canonical set (docs/reference-materials.md §2,\n          # docs/verification/stock-release-20250402.md) -- catches a\n          # silently-empty stock archive passing the (vacuous) diff above.",
    "rationale": "4 lines, single coherent point, already doc-cited; splitting further would not meaningfully shrink it."
   },
   {
    "startLine": 778,
    "endLine": 783,
    "topic": "SHA256SUMS generated once now (no publish-side append)",
    "category": "split",
    "docsSection": "Retired two-image release design (historical contrast)",
    "loadBearing": false,
    "replacement": "      # Generated once here (main + kernel-variant assets) — no publish-side\n      # append anymore (the old two-image design needed one). See docs/ci.md#old-two-image-design-retired.",
    "rationale": "One of several mentions of the retired two-image design scattered through this file (also lines 138, 965-969); consolidate to one doc section instead of retelling it each time."
   },
   {
    "startLine": 793,
    "endLine": 805,
    "topic": "sdcard images are standalone assets, never in the 7z or db.json",
    "category": "split",
    "docsSection": "sdcard images are standalone assets (not in db.json)",
    "loadBearing": true,
    "replacement": "      ##########################################################################\n      # sdcard images are standalone — NEVER in release_YYYYMMDD.7z, NEVER\n      # referenced by db.json (ADR 0017/0020). See docs/ci.md#sdcard-contract.\n      ##########################################################################",
    "rationale": "The contract boundary (sdcard assets are never part of the Downloader's update path) is the load-bearing fact a future edit must not blur; the build-reuse mechanics move."
   },
   {
    "startLine": 807,
    "endLine": 821,
    "topic": "sdcard build: cache reuse + the full-dispatch skip condition",
    "category": "split",
    "docsSection": "sdcard full-dispatch skip/reuse condition",
    "loadBearing": true,
    "replacement": "      # Skipped ONLY on an opt-in full-sdcard dispatch (redundant rebuild against\n      # an already-released tag) — a needless second relink risks the job's\n      # timeout (docs/ci.md#sdcard-timing-6h-cap). See docs/ci.md#sdcard-full-dispatch-skip.",
    "rationale": "Ties back into the 6h-cap concern from the timeout comment above — keep that warning live here too, move the cache-handoff mechanics (fetch-sdcard-payload.sh reconciliation, STOCK_* env inheritance) to docs."
   },
   {
    "startLine": 834,
    "endLine": 837,
    "topic": "Static offline sdcard contract check",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Static, offline contract check (partition table, uboot.img head, FAT32\n      # payload inventory vs docs/verification/sdcard-payload.md). STOCK_UBOOT_*\n      # come from the job env for the default uboot head compare. Same skip\n      # condition as the build step it verifies.",
    "rationale": "Already terse (4 lines), factual, doc-cited; stays as-is."
   },
   {
    "startLine": 842,
    "endLine": 845,
    "topic": "Opt-in full sdcard build step",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Opt-in only: the full variant bundles _Console cores (much larger, needs\n      # the GitHub Contents API). Runs only on a workflow_dispatch with the input\n      # set. Reuses the same warm STAGE_DIR (stock archive + installer builds\n      # already there), just adds the cores; fetch-sdcard-payload.sh reconciles.",
    "rationale": "Already terse (4 lines), factual; stays as-is."
   },
   {
    "startLine": 861,
    "endLine": 863,
    "topic": "Stage sdcard image(s) into dist/",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # Into dist/ so the existing \"Upload release asset bundle\" step carries them\n      # to the publish job. sdcard-full.img.xz is only present when the opt-in\n      # step above ran.",
    "rationale": "Already terse (3 lines), stays as-is."
   },
   {
    "startLine": 867,
    "endLine": 871,
    "topic": "Dist staging: which sdcard variant(s) actually landed",
    "category": "split",
    "docsSection": "sdcard full-dispatch skip/reuse condition",
    "loadBearing": false,
    "replacement": "      # Stages whichever variant(s) this run built; `gh release upload --clobber`\n      # then refreshes only those, leaving other assets intact.",
    "rationale": "Explains --clobber's partial-refresh behavior in the assets array; short warning plus pointer covers it."
   },
   {
    "startLine": 880,
    "endLine": 883,
    "topic": "Release overview job-summary step banner",
    "category": "keep",
    "docsSection": "",
    "loadBearing": false,
    "replacement": "      # --- Release overview on the run's Summary page ------------------------\n      # The tag, the internal version, the archive + image sizes, the full\n      # SHA256SUMS, and -- most importantly -- the draft/db.json coupling a\n      # human needs to understand before clicking Publish.",
    "rationale": "Already terse (4 lines), single coherent point about what the summary page must convey; stays as-is."
   },
   {
    "startLine": 928,
    "endLine": 930,
    "topic": "sha-named release bundle upload, pointer to naming trap",
    "category": "move",
    "docsSection": "Artifact naming: sha vs ref_name ('/' trap)",
    "loadBearing": false,
    "replacement": "      # sha-named — only `publish` (same run) consumes this. See docs/ci.md#artifact-naming-slash-trap.",
    "rationale": "Third occurrence of the same short cross-reference in this file; repoint all of them at the one consolidated doc section instead of an in-file reference that can go stale on reordering."
   },
   {
    "startLine": 939,
    "endLine": 947,
    "topic": "publish job: separate job for permission scoping",
    "category": "split",
    "docsSection": "publish job scope and permissions",
    "loadBearing": false,
    "replacement": "  # ============================================================================\n  # publish — attests provenance + creates the GitHub Release. Separate job:\n  # needs write scopes (contents/id-token/attestations) the build jobs must NOT\n  # have. See docs/ci.md#publish-job-scope.\n  # ============================================================================",
    "rationale": "Structural rationale for why this is its own job; a short pointer preserves the reasoning without the full elaboration of `gh`/OIDC mechanics."
   },
   {
    "startLine": 953,
    "endLine": 956,
    "topic": "publish only fires for a tag ref",
    "category": "split",
    "docsSection": "publish job scope and permissions",
    "loadBearing": true,
    "replacement": "    # Guards against a non-tag workflow_dispatch (full-sdcard opt-in) minting a\n    # branch-named release. See docs/ci.md#publish-job-scope.",
    "rationale": "Guards a real failure mode (a workflow_dispatch on a branch accidentally creating a branch-named release); the warning stays, the elaboration moves."
   },
   {
    "startLine": 965,
    "endLine": 969,
    "topic": "ONE bundle now, no publish-side merge/append",
    "category": "split",
    "docsSection": "Retired two-image release design (historical contrast)",
    "loadBearing": false,
    "replacement": "      # ONE bundle — nothing to merge/append here (the old two-image design\n      # needed a second download+append at this point). See docs/ci.md#old-two-image-design-retired.",
    "rationale": "Another mention of the retired two-image design; consolidate with the other two occurrences (lines 136-139, 778-783) into one doc section."
   },
   {
    "startLine": 979,
    "endLine": 984,
    "topic": "Attestation covers only the shipped binaries, not all of dist/",
    "category": "split",
    "docsSection": "Build-provenance attestation scope",
    "loadBearing": true,
    "replacement": "      # Attests only the shipped binaries (linux.img, zImage_dtb, zImage_dtb-rt)\n      # — not dist/'s metadata files. See docs/ci.md#attest-provenance-scope.",
    "rationale": "The scope of what gets attested is a deliberate, non-obvious choice that a careless 'attest the whole dist/ dir' edit could silently widen; keep the warning inline."
   },
   {
    "startLine": 993,
    "endLine": 1002,
    "topic": "Release created as a DRAFT — the human-approval gate before db.json publishes",
    "category": "split",
    "docsSection": "Draft release gate (publish-db.yml trigger)",
    "loadBearing": true,
    "replacement": "      # Created as a DRAFT, deliberately: publish-db.yml only fires on a\n      # published (non-draft) release — this is the human-approval gate between\n      # \"CI built an image\" and \"every subscribed MiSTer is offered it\".\n      # See docs/ci.md#release-draft-gate.",
    "rationale": "This is the gate between an automated build and every device in the field being offered an update — the warning that draft-vs-published controls db.json publication must stay; the TASKS.md P4.6 posture justification moves."
   },
   {
    "startLine": 1006,
    "endLine": 1019,
    "topic": "GH_REPO required — the inherited publish-job repo-resolution bug",
    "category": "split",
    "docsSection": "GH_REPO bug (gh repo resolution without checkout)",
    "loadBearing": true,
    "replacement": "          # GH_REPO required: no checkout here, and gh ignores $GITHUB_REPOSITORY —\n          # without it every `gh release` call dies at repo-resolution and a green\n          # ~9h build silently produces NO release. See docs/ci.md#gh-repo-bug.",
    "rationale": "Names a real inherited bug (present in origin/master's publish job, fixed here) with a severe and silent failure mode. The warning is exactly what must survive at the code; the failure-mechanism walkthrough and provenance note move to docs."
   },
   {
    "startLine": 1025,
    "endLine": 1035,
    "topic": "Release asset list: RT assets unconditional, sdcard assets conditional",
    "category": "split",
    "docsSection": "Release asset list construction (RT unconditional, sdcard conditional, upsert)",
    "loadBearing": true,
    "replacement": "          # RT assets are listed UNconditionally (must fail loud if missing);\n          # sdcard assets are conditional on what this run actually staged.\n          # See docs/ci.md#release-assets-array.",
    "rationale": "The asymmetry between the two asset groups is easy to get backwards in a future edit (e.g. making RT conditional would silently hide a real build failure); keep the rule inline."
   },
   {
    "startLine": 1053,
    "endLine": 1058,
    "topic": "Upsert, not create-only, for gh release",
    "category": "split",
    "docsSection": "Release asset list construction (RT unconditional, sdcard conditional, upsert)",
    "loadBearing": true,
    "replacement": "          # Upsert, not create-only: an opt-in full-sdcard dispatch runs against\n          # an EXISTING draft and must ADD to it. See docs/ci.md#release-assets-array.",
    "rationale": "Explains why the script can't just always `gh release create` (the opt-in dispatch targets an already-existing draft release); the warning stays, the walkthrough moves."
   }
  ],
  "file": ".github/workflows/release.yml",
  "totals": {
   "keep": 25,
   "move": 4,
   "split": 39,
   "drop": 0,
   "commentLinesBefore": 463,
   "commentLinesAfter": 170
  },
  "proposedDocSections": [
   "Release consumers (three downstream actors)",
   "Why release.yml rebuilds and re-verifies instead of adopting build.yml's run",
   "Stock payload sourcing & verification order",
   "Open decision: third-party stock-payload dependency",
   "Tag convention (unratified)",
   "MiSTer.version / release filename / db.json coupling (load-bearing)",
   "Kernel-variant matrix (build-kernel) design",
   "Artifact naming: sha vs ref_name ('/' trap)",
   "sdcard build timing and the 6-hour runner cap",
   "sdcard images are standalone assets (not in db.json)",
   "sdcard full-dispatch skip/reuse condition",
   "Release job apt packages (host-only tools)",
   "legal-info.tar.gz: host-sources exclusion and the 2 GiB asset cap",
   "dist/ layout contract",
   "7z l -slt output parsing trap",
   "Retired two-image release design (historical contrast)",
   "publish job scope and permissions",
   "Build-provenance attestation scope",
   "Draft release gate (publish-db.yml trigger)",
   "GH_REPO bug (gh repo resolution without checkout)",
   "Release asset list construction (RT unconditional, sdcard conditional, upsert)",
   "No container / disk reclaim (shared with reproducibility.yml)",
   "ABI/SONAME check behavior in clean checkout",
   "release build job overview",
   "Manual workflow_dispatch: opt-in full sdcard variant"
  ]
 },
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 4,
    "topic": "File banner + one-line title",
    "category": "keep",
    "replacement": "################################################################################\n#\n# renovate-hash-sync.yml -- refresh companion tarball/kernel hashes on a\n# Renovate PR (TASKS.md P4.6).",
    "rationale": "Already terse; a title needs no relocation.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 5,
    "endLine": 12,
    "topic": "Problem statement: Renovate can't compute sha256 itself",
    "category": "move",
    "replacement": "# Renovate can bump a pin but can't compute its companion sha256 itself. See docs/ci.md#renovate-hash-sync-overview.",
    "rationale": "Pure motivating context, no trap/incident tied to a specific line of code below it -- safe to relocate wholesale.",
    "loadBearing": false,
    "docsSection": "Renovate hash sync — why it exists & trigger design"
   },
   {
    "startLine": 13,
    "endLine": 52,
    "topic": "4 handled cases and why each is safe to automate",
    "category": "move",
    "replacement": "# What's automated & why each case (1-4: packages/kernel/lzma-sdk/sdcard) is safe. See docs/ci.md#renovate-hash-sync-safety-model.",
    "rationale": "Long declarative safety rationale (trust-model reasoning), not an incident story; each case gets its own step-header comment right where it's implemented, so the full write-up belongs in one doc section rather than duplicated at the header too.",
    "loadBearing": false,
    "docsSection": "Renovate hash sync — safety model per case (1–4)"
   },
   {
    "startLine": 54,
    "endLine": 71,
    "topic": "What this workflow deliberately does NOT handle",
    "category": "move",
    "replacement": "# Deliberately NOT automated: BUILDROOT_SHA256, cabextract, linux-firmware-extra, xow-firmware. See docs/ci.md#renovate-hash-sync-not-automated.",
    "rationale": "Explains a design boundary, not a trap in this file's own code -- the reasoning (signed-manifest-only trust for Buildroot, no machine-readable feed for the others) lives more usefully alongside the safety-model doc.",
    "loadBearing": false,
    "docsSection": "Renovate hash sync — deliberately not automated"
   },
   {
    "startLine": 73,
    "endLine": 80,
    "topic": "Automatic trigger scope + idempotency guarantee",
    "category": "move",
    "replacement": "# Runs only for Renovate's own same-repo PRs on files it knows how to fix; idempotent. See docs/ci.md#renovate-hash-sync-overview.",
    "rationale": "Design description, restated more concretely later at the job-level `if:` gate (line 178) -- keep the pointer once, put the prose in docs.",
    "loadBearing": false,
    "docsSection": "Renovate hash sync — why it exists & trigger design"
   },
   {
    "startLine": 82,
    "endLine": 96,
    "topic": "Manual workflow_dispatch escape hatch + run 29669946883 incident (header copy)",
    "category": "move",
    "replacement": "# Manual workflow_dispatch escape hatch exists because nothing else can re-drive this. See docs/ci.md#renovate-hash-sync-dispatch-trap.",
    "rationale": "This is a near-verbatim duplicate of the comment right above the actual `workflow_dispatch:` trigger (line 144-156), which is the better place for the surviving warning -- so the header copy fully relocates rather than also carrying a split warning.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — manual dispatch escape hatch (run 29669946883)"
   },
   {
    "startLine": 98,
    "endLine": 110,
    "topic": "PARTIALLY VERIFIED / UNPROVEN status, PR #41, bug #42",
    "category": "split",
    "replacement": "# STATUS: only the kernel step is proven (PR #41); rest UNPROVEN. A green run can mean \"silently skipped\" (bug #42). See docs/ci.md#renovate-hash-sync-verification-status.",
    "rationale": "Point-in-time verification log with a concrete named bug (#42) and a warning a future editor must not miss (green != correct here); the dated narrative moves, the warning stays.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — verification status (PR #41, bug #42)"
   },
   {
    "startLine": 112,
    "endLine": 112,
    "topic": "Banner closing delimiter",
    "category": "keep",
    "replacement": "################################################################################",
    "rationale": "Pure decoration, matches the opening banner.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 121,
    "endLine": 127,
    "topic": "Why mister_kernel_defconfig also triggers; why mister_rt.fragment is excluded",
    "category": "split",
    "replacement": "# mister_kernel_defconfig also triggers (mirrors main's kernel stanza); mister_rt.fragment excluded -- its hash can't auto-refresh. See docs/ci.md#renovate-hash-sync-not-automated.",
    "rationale": "Two facts a maintainer editing this paths: list must not lose (which files matter, why RT is absent); the underlying reasoning (no signed -rc manifest, TOFU procedure) is stated fully elsewhere in this same file (line 418) and belongs in the shared not-automated doc section rather than a third copy.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — deliberately not automated"
   },
   {
    "startLine": 144,
    "endLine": 156,
    "topic": "Manual dispatch escape hatch + run 29669946883 incident + dispatch-from vs branch-at trap",
    "category": "split",
    "replacement": "# Escape hatch: nothing else can re-drive this workflow after a bug fix (run 29669946883 replayed old code 3x). `branch` != the ref you dispatch from. See docs/ci.md#renovate-hash-sync-dispatch-trap.",
    "rationale": "This is the load-bearing copy of the run-29669946883 story (the header's copy at line 82 now just points here) and names the exact trap -- conflating dispatch-from-ref with the branch-to-fix input -- that a future maintainer could reintroduce.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — manual dispatch escape hatch (run 29669946883)"
   },
   {
    "startLine": 165,
    "endLine": 167,
    "topic": "concurrency group PR-number-or-branch fallback",
    "category": "keep",
    "replacement": "  # pull_request runs key on the PR number; dispatch runs key on the target\n  # branch. Without the fallback every manual run would share one empty-suffix\n  # group and cancel the previous one.",
    "rationale": "Already terse (3 lines) and sits directly on the line it explains.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 178,
    "endLine": 182,
    "topic": "Job-level gate: fork exclusion + automatic-vs-manual privilege parity",
    "category": "split",
    "replacement": "# Never runs for a fork PR (no write-back access) or a non-Renovate PR. See docs/ci.md#renovate-hash-sync-overview.",
    "rationale": "The fork-write-access reasoning duplicates the header's trigger paragraph (line 73-80, now itself moved) -- one doc copy, short warning kept where the actual `if:` condition lives.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — why it exists & trigger design"
   },
   {
    "startLine": 189,
    "endLine": 193,
    "topic": "TARGET_BRANCH resolved once as a shell var (injection rationale, occurrence 1 of 2)",
    "category": "split",
    "replacement": "# Resolved once as a shell var, not inline ${{ }} -- closes a script-injection vector. See docs/ci.md#branch-name-injection.",
    "rationale": "Same injection rationale is repeated verbatim in spirit at the final push step (line 602) -- consolidate into one doc section, keep a short warning at both use sites since both are places a future edit could reintroduce the vector.",
    "loadBearing": true,
    "docsSection": "Branch name is attacker-controlled input (script-injection pattern)"
   },
   {
    "startLine": 198,
    "endLine": 201,
    "topic": "Refuse the default branch outright",
    "category": "split",
    "replacement": "# Refuse the default branch outright -- a mistyped `branch` input must not land an unreviewed commit there. See docs/ci.md#renovate-hash-sync-branch-validation.",
    "rationale": "Names the concrete failure this guards against (unreviewed auto-commit to master); the automatic-path-can't-hit-this aside is background detail that moves.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — branch name validation & default-branch guard"
   },
   {
    "startLine": 213,
    "endLine": 215,
    "topic": "Syntactic branch-name validation runs before checkout",
    "category": "keep",
    "replacement": "          # Syntactic validation first: rejects empty, embedded spaces, \"..\",\n          # a leading \"-\", and the other things git considers malformed. Runs\n          # before checkout, which is fine -- check-ref-format needs no repo.",
    "rationale": "Already terse (3 lines), directly perched on the git check-ref-format call it explains.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 221,
    "endLine": 235,
    "topic": "Ref-namespace bypass trap: refs/heads/x and heads/x resolve to x",
    "category": "split",
    "replacement": "# REJECT ref-namespace forms (refs/heads/x, heads/x) rather than normalise -- both resolve to x and would bypass the check below. See docs/ci.md#renovate-hash-sync-branch-validation.",
    "rationale": "A real verified trap (git push --dry-run showed both alt spellings reach master) with a specific reason normalising was rejected over rejecting -- the warning must survive at the `case` statement it guards; the dry-run evidence and $GITHUB_ENV aside move to docs.",
    "loadBearing": true,
    "docsSection": "Renovate hash sync — branch name validation & default-branch guard"
   },
   {
    "startLine": 251,
    "endLine": 254,
    "topic": "actions/checkout pin kept consistent with other workflows",
    "category": "move",
    "replacement": "# Same actions/checkout pin as every workflow -- keeps Renovate's manager tracking ONE dependency. See docs/ci.md#pin-conventions.",
    "rationale": "Repo-wide convention rationale, not specific to this file's logic or a past incident -- a good candidate for a shared doc section other workflow files can also point at.",
    "loadBearing": false,
    "docsSection": "actions/checkout pin convention"
   },
   {
    "startLine": 258,
    "endLine": 260,
    "topic": "ref: is the PR head branch / dispatch input, not this file's own ref",
    "category": "keep",
    "replacement": "          # The PR head branch, or the dispatch input -- NOT the ref this\n          # workflow definition came from. On a manual run those differ by\n          # design (see the workflow_dispatch note above).",
    "rationale": "Already terse (3 lines) and sits exactly on the `ref:` field it explains.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 264,
    "endLine": 277,
    "topic": "Step 1: generic github-package loop, naming convention, first-line-only rewrite rule",
    "category": "split",
    "replacement": "# Generic loop over packages/libchdr; only the FIRST sha256 line is rewritten, other lines are left for a human. See docs/ci.md#companion-hash-first-line-only.",
    "rationale": "The 'only the first sha256 line' rule is repeated near-verbatim in steps 2 and 3 (lines 336, 465) -- consolidate to one doc section; the tarball-naming-convention research (checked against every existing .hash file) is useful but not needed to safely edit this step.",
    "loadBearing": true,
    "docsSection": "Companion .hash files: only the first sha256 line is machine-owned"
   },
   {
    "startLine": 336,
    "endLine": 339,
    "topic": "Step 2: only linux.hash's sha256 line is touched",
    "category": "move",
    "replacement": "# Rewrites only linux.hash's sha256 data line; the header comment above it is preserved. See docs/ci.md#companion-hash-first-line-only.",
    "rationale": "Third(ish) restatement of the same first-line-only rule as step 1 -- dedup to one doc section.",
    "loadBearing": false,
    "docsSection": "Companion .hash files: only the first sha256 line is machine-owned"
   },
   {
    "startLine": 351,
    "endLine": 359,
    "topic": "Anchored-grep bug story: unanchored match on defconfig comment produced a 2-line $kver and a malformed URL (bug #42)",
    "category": "split",
    "replacement": "# ANCHOR THIS GREP + tail -1 -- an unanchored match returns TWO lines (defconfig quotes the value in a comment) and broke the URL (bug #42). See docs/ci.md#renovate-hash-sync-kernel-grep-bug.",
    "rationale": "This is THE named regression (bug #42, silent-URL-corruption failure mode) the task called out; the warning telling a future editor not to un-anchor the grep must stay directly on that grep line, the full incident narrative moves.",
    "loadBearing": true,
    "docsSection": "Kernel step: anchored-grep bug (#42) and related traps"
   },
   {
    "startLine": 360,
    "endLine": 365,
    "topic": "`|| true` is load-bearing under set -euo pipefail (silent-abort trap)",
    "category": "split",
    "replacement": "# `|| true` is load-bearing here -- without it, set -euo pipefail aborts silently on no-match. See docs/ci.md#renovate-hash-sync-kernel-grep-bug.",
    "rationale": "A classic 'looks removable' footgun: deleting the `|| true` would make a no-match fail with zero diagnostic instead of hitting the explicit empty-check below. The mechanism explanation (why pipefail+plain-assignment triggers set -e) moves; the warning stays.",
    "loadBearing": true,
    "docsSection": "Kernel step: anchored-grep bug (#42) and related traps"
   },
   {
    "startLine": 373,
    "endLine": 376,
    "topic": "Malformed version fails loudly instead of warn-and-skip",
    "category": "move",
    "replacement": "# A malformed version is a bug in this script, not a network hiccup -- fail loudly, don't warn-and-skip. See docs/ci.md#renovate-hash-sync-kernel-grep-bug.",
    "rationale": "Design rationale for why this one check diverges from the warn-and-skip pattern used elsewhere in the same step; useful context but no separate named incident beyond bug #42 already covered above, so a short pointer suffices.",
    "loadBearing": false,
    "docsSection": "Kernel step: anchored-grep bug (#42) and related traps"
   },
   {
    "startLine": 392,
    "endLine": 393,
    "topic": "kernel.org manifest line format example",
    "category": "keep",
    "replacement": "          # kernel.org's manifest lines look like:\n          #   <sha256hash>  linux-6.18.38.tar.xz",
    "rationale": "Already terse (2 lines), a concrete format example that aids the very next grep line.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 403,
    "endLine": 420,
    "topic": "RT-line clobber story: major-series matching required, first-line/extension-only match overwrote the RT entry (verified)",
    "category": "split",
    "replacement": "# Match the major series (linux-<major>.*), never the first sha256 line -- that clobbered the RT entry (verified). See docs/ci.md#renovate-hash-sync-rt-line-clobber.",
    "rationale": "The other named incident the task called out: a verified reproduction where a 6.18 bump overwrote the RT beta's line and produced two 6.18 entries. The warning identifying the exact wrong pattern (first-line, or extension-only) must stay at the regex it protects; the reproduction narrative and the -rc-vs-stable-release timing detail move.",
    "loadBearing": true,
    "docsSection": "Kernel step: RT-line clobber trap (major-series matching)"
   },
   {
    "startLine": 424,
    "endLine": 424,
    "topic": "Refuse to guess when 2+ lines match the major-series pattern",
    "category": "keep",
    "replacement": "          # If two lines somehow match, do not guess which one this pin owns.",
    "rationale": "Already a single line, directly on the nmatch check it explains.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 435,
    "endLine": 438,
    "topic": "awk -v regex-escaping trap: rebuilding the regex inside awk -v eats backslashes",
    "category": "split",
    "replacement": "# Match by exact string, not a regex rebuilt inside awk -v -- awk -v eats backslash escapes (`\\.` -> any char). See docs/ci.md#renovate-hash-sync-kernel-grep-bug.",
    "rationale": "A subtle, generically-recurring gotcha (awk -v silently degrading a regex) that a 'simplification' pass could easily reintroduce by inlining the pattern -- the one-line warning must stay; full explanation moves.",
    "loadBearing": true,
    "docsSection": "Kernel step: anchored-grep bug (#42) and related traps"
   },
   {
    "startLine": 454,
    "endLine": 468,
    "topic": "Step 3: lzma-sdk is bespoke (release asset, not commit archive); filename derivation; first-line-only rule",
    "category": "split",
    "replacement": "# Bespoke, not the step-1 loop: a release ASSET, filename 7z<ver-no-dots>-src.tar.xz. Only first sha256 line rewritten. See docs/ci.md#companion-hash-first-line-only.",
    "rationale": "Keeps the two facts needed to safely touch this step (why it's not folded into the generic loop, and the exact filename formula); the trust-model paragraph duplicates case 3 of the header (already moved) and the first-line-only rule duplicates steps 1/2.",
    "loadBearing": true,
    "docsSection": "Companion .hash files: only the first sha256 line is machine-owned"
   },
   {
    "startLine": 520,
    "endLine": 529,
    "topic": "Step 4: sdcard payload pins update_all.sh/wifi.sh only; PINNED_CORES_COMMIT unhandled; no-heredoc reason",
    "category": "split",
    "replacement": "# Pins update_all.sh + wifi.sh only -- PINNED_CORES_COMMIT has no companion hash. No heredoc (YAML-indent unsafe here). See docs/ci.md#renovate-hash-sync-not-automated.",
    "rationale": "The PINNED_CORES_COMMIT-is-unhandled fact duplicates case 4 of the header (already moved) -- one doc copy; the no-heredoc implementation note is kept inline since it explains the very next 'set --' line's unusual shape.",
    "loadBearing": false,
    "docsSection": "Renovate hash sync — deliberately not automated"
   },
   {
    "startLine": 540,
    "endLine": 540,
    "topic": "shellcheck disable for intentional word-splitting",
    "category": "keep",
    "replacement": "            # shellcheck disable=SC2086",
    "rationale": "Standard single-line shellcheck suppression comment.",
    "loadBearing": false,
    "docsSection": ""
   },
   {
    "startLine": 602,
    "endLine": 606,
    "topic": "Final push uses a shell var, not ${{ }}, for TARGET_BRANCH (injection rationale, occurrence 2 of 2)",
    "category": "split",
    "replacement": "# Shell var, not ${{ }} interpolation -- TARGET_BRANCH is attacker-controlled text on a PR. See docs/ci.md#branch-name-injection.",
    "rationale": "Same injection pattern as line 189-193, stated a second time at the actual `git push`; consolidated to one doc section but the warning stays at both use sites since both are places a future edit (e.g. 'simplify this to ${{ env.TARGET_BRANCH }}') could reopen the hole. The `--` before the pushspec detail moves.",
    "loadBearing": true,
    "docsSection": "Branch name is attacker-controlled input (script-injection pattern)"
   }
  ],
  "file": ".github/workflows/renovate-hash-sync.yml",
  "totals": {
   "keep": 8,
   "move": 8,
   "split": 15,
   "drop": 0,
   "commentLinesBefore": 267,
   "commentLinesAfter": 41
  },
  "proposedDocSections": [
   "Renovate hash sync — why it exists & trigger design",
   "Renovate hash sync — safety model per case (1–4)",
   "Renovate hash sync — deliberately not automated",
   "Renovate hash sync — manual dispatch escape hatch (run 29669946883)",
   "Renovate hash sync — verification status (PR #41, bug #42)",
   "Renovate hash sync — branch name validation & default-branch guard",
   "Branch name is attacker-controlled input (script-injection pattern)",
   "actions/checkout pin convention",
   "Companion .hash files: only the first sha256 line is machine-owned",
   "Kernel step: anchored-grep bug (#42) and related traps",
   "Kernel step: RT-line clobber trap (major-series matching)"
  ]
 },
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 9,
    "topic": "file header — what build.yml does",
    "category": "move",
    "replacement": "################################################################################\n#\n# build.yml — CI build workflow (TASKS.md P4.1, PLAN.md §9 \"Build\"). See docs/ci.md#build-yml-overview.",
    "rationale": "Pure descriptive overview (builds via make all, runs parity+ABI, uploads). No trap, no incident — safe to relocate wholesale.",
    "loadBearing": false
   },
   {
    "startLine": 10,
    "endLine": 19,
    "topic": "kernel-variant matrix architecture summary",
    "category": "move",
    "replacement": "# Kernel-variant matrix (build-kernel) runs first; build merges every leg's\n# modules into the ONE shipped linux.img. See docs/ci.md#kernel-variant-matrix.\n#",
    "rationale": "Duplicates the build-kernel job's own comment (line ~188) almost exactly. Keep one canonical inline copy (at the job) and relocate the header's restatement.",
    "loadBearing": false
   },
   {
    "startLine": 20,
    "endLine": 27,
    "topic": "shared buildroot-build action + 5-failed-runs incident",
    "category": "split",
    "replacement": "# Build recipe lives in .github/actions/buildroot-build, SHARED with release.yml\n# and reproducibility.yml — read its header before changing how the build runs.\n# (Drift here once cost build.yml 5 failed runs.) See docs/ci.md#shared-build-recipe.\n#",
    "rationale": "Named past incident (5 failed runs) plus an active trap (editing the shared action affects 3 workflows). The warning must stay at the code; the incident narrative moves.",
    "loadBearing": true
   },
   {
    "startLine": 28,
    "endLine": 33,
    "topic": "ABI checker overview + ci-tests.sh redundancy note",
    "category": "move",
    "replacement": "# ABI/SONAME checker: scripts/check-abi.sh (P2.2); ci-tests.sh intentionally\n# re-checks the two highest-value gates (A-10/A-22). See docs/ci.md#abi-checker-overlap.\n#\n################################################################################",
    "rationale": "Explains why two scripts overlap on A-10/A-22 — a real, non-obvious fact, but purely explanatory, no warning needed at this location since the ABI step itself (line ~503) carries its own inline note.",
    "loadBearing": false
   },
   {
    "startLine": 37,
    "endLine": 54,
    "topic": "3h20m serialization cost trade + '3 places hard-code kernel legs ran' trap",
    "category": "split",
    "replacement": "# ~3h20m cold. Do not skip/demote `build-kernel` with a bare `if:` — 3 other\n# places hard-code \"the kernel legs always ran\" (build's own `if:`, the\n# `status` truth table, and the zero-modules-tars guard in \"Populate the extra-\n# modules overlay\"). See docs/ci.md#kernel-gate-cascade before changing this.",
    "rationale": "This is exactly the kind of trap the task calls out by name: a future maintainer demoting build-kernel to push-only WILL break 3 other places unless warned here first.",
    "loadBearing": true
   },
   {
    "startLine": 55,
    "endLine": 66,
    "topic": "push trigger scoped to master — double-build incident",
    "category": "split",
    "replacement": "# 1. `push` is scoped to master only — do not widen this back to a bare\n#    `push:`. It once double-built every PR commit (~6h40m/commit, confirmed\n#    from run history: every SHA built twice). See docs/ci.md#push-trigger-scope.",
    "rationale": "Named past incident with quantified cost (6h40m/commit) and a concrete confirmation method ('every SHA in gh run list appeared twice'). Warning stays, forensic detail moves.",
    "loadBearing": true
   },
   {
    "startLine": 67,
    "endLine": 84,
    "topic": "paths-ignore branch-protection trap + denylist-vs-allowlist rationale",
    "category": "split",
    "replacement": "# 2. Doc-only skip is decided by the `gate` job, NOT `paths-ignore:` — that\n#    stops the workflow from reporting at all, wedging a required \"Build\"\n#    branch-protection check forever. `gate` is a DENYLIST, not an allowlist —\n#    see docs/ci.md#gate-vs-paths-ignore for why the allowlist direction is a trap.",
    "rationale": "This is the paths-ignore trap the task explicitly flagged. The warning (don't use paths-ignore, don't invert to an allowlist) must survive; the extended reasoning/analogy moves.",
    "loadBearing": true
   },
   {
    "startLine": 92,
    "endLine": 93,
    "topic": "concurrency cancel-in-progress rationale",
    "category": "keep",
    "replacement": "# Cancel a superseded run for the same ref (e.g. a new push to an open PR)\n# rather than burning CI minutes finishing a build nobody wants anymore.",
    "rationale": "Already 2 lines, single point, no incident.",
    "loadBearing": false
   },
   {
    "startLine": 98,
    "endLine": 99,
    "topic": "least-privilege permissions rationale",
    "category": "keep",
    "replacement": "# Least privilege: this workflow only reads the repo and uploads artifacts,\n# both covered by the default `contents: read` — no token write scope needed.",
    "rationale": "Already 2 lines, single point.",
    "loadBearing": false
   },
   {
    "startLine": 104,
    "endLine": 108,
    "topic": "gate job cost + plain-git-not-an-action design choice",
    "category": "move",
    "replacement": "# Costs seconds; decides whether the 3h20m job below is worth starting.\n# See docs/ci.md#gate-job-design (why plain git, not a paths-filter action).",
    "rationale": "Design rationale (avoid a third-party supply-chain dependency) with no active trap or incident tied to this exact line — safe to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 118,
    "endLine": 119,
    "topic": "fetch-depth 0 rationale",
    "category": "keep",
    "replacement": "# Full history: the diff range below reaches back to a base commit\n# that a shallow clone would not have.",
    "rationale": "Already 2 lines.",
    "loadBearing": false
   },
   {
    "startLine": 131,
    "endLine": 135,
    "topic": "is_doc() denylist + 'verified before writing this' claim",
    "category": "split",
    "replacement": "# Doc-only paths that provably cannot affect a compile (docs/*, *.md, LICENSE,\n# .editorconfig, .gitignore). Verified: no .md is read by the Makefile/\n# external.mk/Config.in/post-build.sh — re-verify before trusting this again.\n# See docs/ci.md#gate-denylist.",
    "rationale": "The 'verified before writing this' claim is load-bearing (the whole denylist depends on it staying true) and must remain as a standing warning to re-check; the rest is descriptive.",
    "loadBearing": true
   },
   {
    "startLine": 143,
    "endLine": 145,
    "topic": "diff-range fallback-to-build policy",
    "category": "keep",
    "replacement": "# Establish a diff range, or give up and build. Every unknown here\n# resolves to \"build\": a wasted run costs minutes, a wrongly-skipped\n# one ships an untested image.",
    "rationale": "Already 3 lines, single terse policy statement.",
    "loadBearing": false
   },
   {
    "startLine": 188,
    "endLine": 194,
    "topic": "build-kernel job architecture + how to add a variant",
    "category": "split",
    "replacement": "# Kernel-variant matrix (ADR 0021 as amended 2026-07-18); see docs/ci.md#kernel-variant-matrix.\n# Adding a variant = one new name here (+ its fragment + Makefile targets).\n# A failed leg auto-skips `build` via `needs` — see `status` for the cascade.",
    "rationale": "The 'how to add a variant' instruction and the needs-cascade pointer are operationally load-bearing at this exact spot; the ADR/architecture narrative (duplicated with the file header) moves.",
    "loadBearing": true
   },
   {
    "startLine": 204,
    "endLine": 209,
    "topic": "240min kernel-leg timeout — honestly unmeasured",
    "category": "split",
    "replacement": "# HONESTLY UNMEASURED — no kernel-only leg has run yet; 240 is an estimated\n# ~2x safety margin. Re-derive from real runs once they exist.\n# See docs/ci.md#kernel-leg-timeout for the derivation.",
    "rationale": "The 'unmeasured, re-derive later' caveat must stay visible next to the number it qualifies; the derivation arithmetic moves.",
    "loadBearing": true
   },
   {
    "startLine": 216,
    "endLine": 217,
    "topic": "no patch lint on this checkout, deferred to build job",
    "category": "keep",
    "replacement": "# No patch-header lint here: `build` lints the one shared patches dir\n# (linux-patches-beta/ is symlinks + a series into it).",
    "rationale": "Already 2 lines.",
    "loadBearing": false
   },
   {
    "startLine": 224,
    "endLine": 230,
    "topic": "kernel overview summary — assert already enforced, uniqueness-guarded glob",
    "category": "split",
    "replacement": "# Best-effort; every fact guarded, none fail the build. Kernel-tree glob is\n# uniqueness-guarded (linux-firmware-*/linux-headers-* must not match) — same\n# assert the Makefile enforces. See docs/ci.md#uniqueness-guarded-globs.",
    "rationale": "The uniqueness-guard warning (a stale sibling tree corrupts the glob match) is a real trap pattern repeated across this file and release.yml — it must survive inline; the 'mirrors the main job' framing moves.",
    "loadBearing": true
   },
   {
    "startLine": 266,
    "endLine": 274,
    "topic": "kernel artifact is inter-job transport + Copilot-review uniqueness guard",
    "category": "split",
    "replacement": "# INTER-JOB TRANSPORT, not a deliverable — uploads on ALL events (PRs\n# included). Both globs are uniqueness-guarded: a stale sibling tree would\n# silently stage the WRONG kernel's files. See docs/ci.md#kernel-artifact-transport.",
    "rationale": "Names a real review finding (Copilot) that caught a silent-wrong-file bug class. The warning must stay; size/byte-count trivia moves.",
    "loadBearing": true
   },
   {
    "startLine": 280,
    "endLine": 285,
    "topic": "nullglob rationale in staging script",
    "category": "split",
    "replacement": "# nullglob: without it, a zero-match glob leaves the literal pattern in the\n# array and the count in the error below would lie. See docs/ci.md#nullglob-staging.",
    "rationale": "The core reason (truthful error counts) must stay next to the shopt call; the cross-reference to the Populate step's identical pattern and the set -u short-circuit detail move.",
    "loadBearing": false
   },
   {
    "startLine": 305,
    "endLine": 307,
    "topic": "depmod'd modules tar rationale",
    "category": "keep",
    "replacement": "# Depmod'd by target-finalize (the whole point of the kernel config's\n# BR2_TARGET_ROOTFS_TAR); tar'd from target/ so the paths inside are\n# exactly what work/extra-modules-overlay/ needs.",
    "rationale": "Already 3 lines.",
    "loadBearing": false
   },
   {
    "startLine": 310,
    "endLine": 312,
    "topic": "manifest-only SBOM for the kernel leg",
    "category": "keep",
    "replacement": "# Manifest-only SBOM, same exclusions and reasoning as the main job's\n# SBOM artifact: a CI push distributes nothing, so the GPL source\n# tarballs stay out (release.yml ships the full kernel-source bundle).",
    "rationale": "Already 3 lines and already written as a pointer to the main SBOM comment — good existing style.",
    "loadBearing": false
   },
   {
    "startLine": 330,
    "endLine": 332,
    "topic": "needs-cascade note on build job",
    "category": "keep",
    "replacement": "# needs on the matrix job aggregates ALL its legs; a failed (or cancelled)\n# leg therefore auto-skips this job -- `status` below translates that skip\n# into \"the kernel leg is the root cause\", not a second failure.",
    "rationale": "Already 3 lines, single point that is itself one of the 'needs-cascade' warnings the header refers to — keep intact at its anchor point.",
    "loadBearing": true
   },
   {
    "startLine": 337,
    "endLine": 344,
    "topic": "300min timeout derived from measured cold build (run 29300460591)",
    "category": "split",
    "replacement": "# 300 min: ~1.5x the measured first cold build (run 29300460591, 3h19m47s).\n# A cold build is NOT the rare case — any toolchain-fingerprint change busts\n# the cross-toolchain cache. See docs/ci.md#build-timeouts.",
    "rationale": "Cites a specific CI run ID as evidence — load-bearing provenance for the number, and duplicated (same run ID) in release.yml and reproducibility.yml, so it becomes ONE docs section. The 'not the rare case' caveat is a live trap warning and stays.",
    "loadBearing": true
   },
   {
    "startLine": 351,
    "endLine": 366,
    "topic": "patch-header lint placement + git-am trap + both series",
    "category": "split",
    "replacement": "# Deliberately BEFORE the build: fails in ~1s instead of after a possible\n# 300-minute build. Guards an interface the build itself cannot — Buildroot's\n# `patch -p1` ignores mail headers, so a malformed `From:` builds green and\n# only breaks `git am` at export time. See docs/ci.md#patch-lint-placement.",
    "rationale": "Documents a real silent-failure mode (patch -p1 vs git am) that CI cannot otherwise catch — a genuine trap that must stay visible at the step; the 'why lint both series' elaboration moves.",
    "loadBearing": true
   },
   {
    "startLine": 370,
    "endLine": 374,
    "topic": "kernel-defconfig lockstep lint",
    "category": "split",
    "replacement": "# Asserts the kernel-only defconfig hasn't drifted from main's toolchain/kernel\n# stanzas (a COPY, easy to forget to mirror). See docs/ci.md#kernel-defconfig-lockstep.",
    "rationale": "The drift trap (defconfig is a manually-mirrored copy) is the load-bearing part; the composite-action cross-reference moves.",
    "loadBearing": true
   },
   {
    "startLine": 378,
    "endLine": 381,
    "topic": "populate extra-modules overlay mechanism",
    "category": "move",
    "replacement": "# Merges kernel-leg module trees into linux.img via BR2_ROOTFS_OVERLAY at\n# target-finalize. See docs/ci.md#kernel-module-overlay.",
    "rationale": "Pure mechanism description, no trap; this is also the canonical copy release.yml points back to ('same mechanism as build.yml's step'), so docs/ci.md becomes the single shared home instead.",
    "loadBearing": false
   },
   {
    "startLine": 395,
    "endLine": 396,
    "topic": "zero-tars is a wiring bug, not empty matrix",
    "category": "keep",
    "replacement": "# Zero tars is a wiring bug, not an empty matrix: this job only runs\n# after build-kernel succeeded, and every leg uploads a modules tar.",
    "rationale": "Already 2 lines.",
    "loadBearing": false
   },
   {
    "startLine": 408,
    "endLine": 408,
    "topic": "one-line summary of what buildroot-build does",
    "category": "keep",
    "replacement": "# Everything from \"prepare the runner\" to \"linux.img + zImage_dtb exist\".",
    "rationale": "Single line already.",
    "loadBearing": false
   },
   {
    "startLine": 414,
    "endLine": 417,
    "topic": "assert overlay merged — silent-miss-must-fail-loudly",
    "category": "keep",
    "replacement": "# A silent overlay miss must fail loudly: if a downloaded kver never made\n# it into the built rootfs (overlay path typo, BR2_ROOTFS_OVERLAY edit),\n# nothing else in this job would notice -- the image builds green, it just\n# ships a kernel whose modules are not there.",
    "rationale": "4 lines, slightly over the 3-line guideline, but it is entirely a single warning with no separable narrative/incident/numbers to relocate — compressing further would blunt the exact failure mode it warns about. Left intact rather than manufacturing a hollow split.",
    "loadBearing": true
   },
   {
    "startLine": 433,
    "endLine": 439,
    "topic": "build-overview job-summary section description",
    "category": "move",
    "replacement": "# --- Build overview on the run's Summary page (best-effort). ---\n# See docs/ci.md#build-summary-step.",
    "rationale": "Pure description of a cosmetic reporting step, no trap.",
    "loadBearing": false
   },
   {
    "startLine": 443,
    "endLine": 445,
    "topic": "kvers lists ALL module trees, proof of overlay merge",
    "category": "keep",
    "replacement": "# ALL module trees in the image, not just the boot kernel's: since the\n# extra-modules overlay this list is the proof the variant trees are\n# really inside linux.img.",
    "rationale": "Already 3 lines.",
    "loadBearing": false
   },
   {
    "startLine": 477,
    "endLine": 481,
    "topic": "CI_TESTS_SKIP_QEMU_SYSTEM flag rationale",
    "category": "split",
    "replacement": "# CI_TESTS_SKIP_QEMU_SYSTEM=1 skips the slow 6-boot QEMU system-kernel check;\n# unset for a full manual/nightly run. See docs/ci.md#parity-suite.",
    "rationale": "The operational instruction (how to get the full suite) must stay actionable at the flag; the rest is background on what ci-tests.sh does.",
    "loadBearing": false
   },
   {
    "startLine": 487,
    "endLine": 493,
    "topic": "if: always() rationale for parity-results upload",
    "category": "split",
    "replacement": "# `if: always()` is deliberate — this artifact matters most when the step\n# above failed. See docs/ci.md#always-upload-rationale.",
    "rationale": "The always()-is-deliberate point is the load-bearing warning (remove it and someone 'simplifying' drops the artifact exactly when it's needed); the annotation/digest mechanics move.",
    "loadBearing": true
   },
   {
    "startLine": 503,
    "endLine": 510,
    "topic": "ABI/SONAME parity checker — what it asserts, A-10/A-22 skip semantics",
    "category": "split",
    "replacement": "# Asserts docs/abi-contract.md §13.1 against the built rootfs; A-10/A-22\n# SKIP (not fail) when the gitignored work/ stock binary is absent.\n# See docs/ci.md#abi-checker-detail.",
    "rationale": "The SKIP-not-FAIL semantics of A-10/A-22 is a trap (a reviewer might mistake 'skipped' for 'passed' or vice versa) and stays; the SONAME/glibc-stub enumeration (already documented in docs/abi-contract.md) moves.",
    "loadBearing": true
   },
   {
    "startLine": 514,
    "endLine": 520,
    "topic": "artifact upload gating (push/dispatch only) + workflow_dispatch-forgotten incident",
    "category": "split",
    "replacement": "# NOT on pull_request (avoids doubling storage per PR revision). workflow_dispatch\n# DOES upload — once forgotten, a manual run finished green but published\n# nothing. See docs/ci.md#artifact-upload-gating.",
    "rationale": "Named past incident ('exactly what happened once workflow_dispatch was added without updating this gate') — must stay as a warning against repeating it when triggers change.",
    "loadBearing": true
   },
   {
    "startLine": 532,
    "endLine": 546,
    "topic": "SBOM legal-info exclusions + 2109MB/24x incident",
    "category": "split",
    "replacement": "# Excludes legal-info/sources + host-sources: with them this artifact was\n# 2109 MB, 24x the images (87 MB), on every push. Full GPL bundle ships from\n# release.yml instead. See docs/ci.md#sbom-legal-info-size.",
    "rationale": "Quantified past incident (2109 MB, 24x) that directly justifies the --exclude flags below it — must stay as the warning against re-adding those paths; the GPL-obligation legal reasoning moves.",
    "loadBearing": true
   },
   {
    "startLine": 566,
    "endLine": 593,
    "topic": "status job rationale + required-check truth table",
    "category": "split",
    "replacement": "# THE JOB TO MARK AS THE REQUIRED STATUS CHECK, not `build` (which skips on\n# doc-only changes — a skipped check never satisfies branch protection).\n#\n#   job succeeded        -> pass   (it compiled, it worked)\n#   all skipped, no need -> pass   (docs only; nothing to compile -- the point)\n#   job failed           -> FAIL   (do not let a skipped-vs-failed confusion\n#                                   turn a broken build into a green check)\n#   job cancelled        -> FAIL   (a superseded run is not evidence)\n#   build skipped because\n#   build-kernel failed  -> FAIL, but NAMED as the needs-cascade: the kernel\n#                           leg is the root cause, build's \"skipped\" is not a\n#                           second, separate workflow bug\n#   gate itself sick     -> FAIL   (if we cannot tell whether a build was\n#                                   needed, we have not established one wasn't)\n#\n# See docs/ci.md#status-job-rationale (ADR 0021 gating, needs.result vs success()/failure()).",
    "rationale": "The truth table is the spec the case-statements below implement — a future maintainer editing that logic must be checked against it, so it stays verbatim rather than moving. Only the ADR-provenance prose and the needs.result-vs-success() aside move.",
    "loadBearing": true
   }
  ],
  "file": ".github/workflows/build.yml",
  "totals": {
   "keep": 12,
   "move": 6,
   "split": 19,
   "drop": 0,
   "commentLinesBefore": 266,
   "commentLinesAfter": 116
  },
  "proposedDocSections": [
   "build-yml-overview",
   "kernel-variant-matrix",
   "shared-build-recipe",
   "abi-checker-overlap",
   "kernel-gate-cascade",
   "push-trigger-scope",
   "gate-vs-paths-ignore",
   "gate-job-design",
   "gate-denylist",
   "kernel-leg-timeout",
   "uniqueness-guarded-globs",
   "kernel-artifact-transport",
   "nullglob-staging",
   "build-timeouts",
   "patch-lint-placement",
   "kernel-defconfig-lockstep",
   "kernel-module-overlay",
   "build-summary-step",
   "parity-suite",
   "always-upload-rationale",
   "abi-checker-detail",
   "artifact-upload-gating",
   "sbom-legal-info-size",
   "status-job-rationale"
  ]
 },
 {
  "file": ".github/workflows/reproducibility.yml",
  "blocks": [
   {
    "startLine": 1,
    "endLine": 25,
    "topic": "File identity, A9 purpose, the P2.5 gap this workflow closes, 2-leg matrix structure",
    "category": "split",
    "docsSection": "reproducibility-workflow",
    "replacement": "################################################################################\n# reproducibility.yml — double-build reproducibility CI (TASKS.md P4.3, A9).\n# Proves two independent builds of the same commit are byte-identical (P2.5\n# only proved this locally). See docs/ci.md#reproducibility-workflow.\n################################################################################",
    "rationale": "Explains what the workflow proves and the specific gap it closes (P2.5 proved idempotency locally over one output/ tree, not across two genuinely independent builds) plus the 2-leg matrix design. Valuable onboarding narrative, not a trap — a maintainer editing the matrix or trigger doesn't need the full P2.5 backstory re-read every time; a short identity comment + pointer suffices at the code, full story moves to docs.",
    "loadBearing": false
   },
   {
    "startLine": 26,
    "endLine": 40,
    "topic": "INPUT caches vs OUTPUT caches — the rule this file must not violate",
    "category": "split",
    "docsSection": "input-vs-output-caches",
    "replacement": "# TRAP: caches below are INPUTS only (toolchain/dl/ccache) — output/target and\n# output/images must NEVER be cached here, or the comparison compares a cache\n# against itself. See docs/ci.md#input-vs-output-caches.",
    "rationale": "This is explicitly flagged in the original text as 'the one rule this file must not violate' — a maintainer who caches output/target or output/images to speed up the job would silently make the reproducibility check meaningless (green even when the underlying build isn't reproducible). Classic split candidate: the warning must survive at the code where someone would add a cache step; the explanation of why toolchain/ccache sharing is safe moves to docs.",
    "loadBearing": true
   },
   {
    "startLine": 41,
    "endLine": 51,
    "topic": "Why linux.img/zImage_dtb themselves are never uploaded, only SHA256SUMS",
    "category": "split",
    "docsSection": "no-image-upload",
    "replacement": "# Only per-leg SHA256SUMS are uploaded, not the images (2x storage/transfer\n# for no benefit; build.yml already ships the bytes). On a mismatch, re-run\n# locally and bisect. See docs/ci.md#no-image-upload.",
    "rationale": "Design rationale a future maintainer might want to overturn ('let's just upload both images so we can diff them directly') — the one-liner preempts that by naming the cost tradeoff and the actual mismatch-investigation procedure inline; the storage-math detail (hundreds of MB, 64-byte hashes) moves to docs.",
    "loadBearing": false
   },
   {
    "startLine": 52,
    "endLine": 60,
    "topic": "Shared build recipe lives in the composite action, near-verbatim-copy incident",
    "category": "move",
    "docsSection": "shared-build-recipe",
    "replacement": "# Build recipe: .github/actions/buildroot-build, shared with build.yml and\n# release.yml. See docs/ci.md#shared-build-recipe (past-drift incident).",
    "rationale": "This exact rationale (three files used to carry near-verbatim copies of the build recipe; fixes to one didn't propagate to the others, silently breaking reproducibility.yml) is duplicated almost verbatim in build.yml's header (lines 20-26) and referenced in release.yml. It belongs as ONE docs/ci.md section that all three workflows point to, not three slightly-drifted retellings — the drift-prone duplication is literally the incident being described.",
    "loadBearing": true
   },
   {
    "startLine": 61,
    "endLine": 66,
    "topic": "Honest status caveat: this workflow has never actually been run",
    "category": "keep",
    "docsSection": "",
    "replacement": "#\n# This workflow has never actually been run — see docs/reproducibility.md\n# for the honest, as-yet-unproven status of the \"no residual nondeterminism\"\n# claim and what would populate that section once CI runs it for real.\n#\n################################################################################",
    "rationale": "Already short (3 content lines) and is a live status fact (not stable narrative) that must stay visible at the point a maintainer would look to see if the claim is proven. It already points to docs/reproducibility.md for detail. No relocation needed.",
    "loadBearing": true
   },
   {
    "startLine": 71,
    "endLine": 78,
    "topic": "MANUAL-ONLY trigger: do not wire to push/pull_request (cost math, gh command)",
    "category": "split",
    "docsSection": "manual-trigger-only",
    "replacement": "  # MANUAL-ONLY (workflow_dispatch): do NOT wire to push/pull_request — 2x\n  # cold builds in parallel with build.yml. Run once caches are warm:\n  #   gh workflow run reproducibility.yml --ref <branch>\n  # See docs/ci.md#manual-trigger-only.",
    "rationale": "A trap-avoidance warning: the obvious 'helpful' change (wire this to push so it runs automatically) would burn two extra cold Buildroot builds in parallel with build.yml every commit. The warning plus the actual command to run it manually must stay inline; the detailed cost arithmetic (which caches, why cold) moves to docs.",
    "loadBearing": true
   },
   {
    "startLine": 81,
    "endLine": 82,
    "topic": "Concurrency: cancel superseded runs, doubly important for a double build",
    "category": "keep",
    "docsSection": "",
    "replacement": "# Cancel a superseded run for the same ref, same as build.yml — a double\n# build is twice the cost of a single one, so this matters even more here.",
    "rationale": "Already 2 lines, terse, no incident detail to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 87,
    "endLine": 89,
    "topic": "Least-privilege permissions justification",
    "category": "keep",
    "docsSection": "",
    "replacement": "# Least privilege: read the repo, upload/download this workflow's own\n# artifacts — both covered by the default contents: read (artifact up/download\n# does not need an elevated token scope; it's scoped to the run itself).",
    "rationale": "Already terse (3 lines). Near-identical short comment appears in build.yml too, but this is boilerplate-short justification, not the kind of long-form duplicated rationale the task calls out for consolidation.",
    "loadBearing": false
   },
   {
    "startLine": 94,
    "endLine": 98,
    "topic": "build job section banner: 2-leg matrix, hash-manifest artifact, pointer to file header",
    "category": "keep",
    "docsSection": "",
    "replacement": "  # ==========================================================================\n  # build — the 2-leg matrix. Each leg is a complete, independent two-stage\n  # Buildroot build from a fresh checkout, ending in a small hash-manifest\n  # artifact instead of the images themselves (see file header).\n  # ==========================================================================",
    "rationale": "Already a pointer ('see file header') rather than restated rationale; 3 content lines. No further relocation needed.",
    "loadBearing": false
   },
   {
    "startLine": 107,
    "endLine": 113,
    "topic": "NO container: — reclaims host software, and does not weaken the double-build comparison",
    "category": "split",
    "docsSection": "no-container",
    "replacement": "    # NO `container:` — see .github/actions/buildroot-build. Does not weaken\n    # the comparison: environment is held constant across both legs either\n    # way. See docs/ci.md#no-container.",
    "rationale": "The base 'no container, host reclaim' rationale is canonical in .github/actions/buildroot-build and already referenced tersely from release.yml ('NO container: — see .github/actions/buildroot-build (same as build)'). This file adds a reproducibility-specific reassurance ('does this weaken the comparison?') worth a one-line answer inline; the generic container/host-reclaim explanation is the duplicated part that becomes one shared docs/ci.md#no-container section referenced from build.yml, release.yml, and here.",
    "loadBearing": false
   },
   {
    "startLine": 115,
    "endLine": 116,
    "topic": "timeout-minutes: 300, matches build.yml's measured cold-build time, per-leg not summed",
    "category": "keep",
    "docsSection": "",
    "replacement": "    # 300 min, matching build.yml — same measured 3h19m cold build (run\n    # 29300460591). The two legs run in parallel, so this is per-leg, not summed.",
    "rationale": "Already 2 lines, already a pointer to build.yml's own measured-number comment (not re-deriving it), includes the run ID citation. Terse and load-bearing as-is.",
    "loadBearing": true
   },
   {
    "startLine": 123,
    "endLine": 138,
    "topic": "Build-the-image step: shared recipe + input/output caches recap + MISTER_VERSION-must-stay-unset trap",
    "category": "split",
    "docsSection": "shared-build-recipe",
    "replacement": "      # Shared recipe (docs/ci.md#shared-build-recipe); caches are inputs only\n      # (docs/ci.md#input-vs-output-caches).\n      #\n      # TRAP: leave MISTER_VERSION UNSET here (ADR 0018) — setting it makes the\n      # two legs disagree the moment they straddle midnight UTC.",
    "rationale": "First two paragraphs (shared recipe, input-vs-output caches) are near-verbatim restatements of the file header's own blocks 1 and 2 — collapsed to one-line pointers rather than re-explained a second time in the same file. The third paragraph is a genuinely distinct, unduplicated TRAP (MISTER_VERSION must stay unset or the two legs diverge at UTC midnight) and is kept as a sharp inline warning per the bias-toward-split-for-traps rule — a maintainer 'fixing' this by setting MISTER_VERSION for consistency with a release build would silently break every reproducibility run that straddles midnight UTC.",
    "loadBearing": true
   },
   {
    "startLine": 142,
    "endLine": 148,
    "topic": "Compute image hashes: must hash from inside output/images/ for bare filenames, not full paths",
    "category": "split",
    "docsSection": "hash-bare-filenames",
    "replacement": "      # --- The whole point of this job: hash the two shipped images ---------\n      # TRAP: hash from INSIDE output/images/, not `sha256sum output/images/...`\n      # — bare filenames make the compare step correct by construction, not by\n      # coincidence of matching paths. See docs/ci.md#hash-bare-filenames.",
    "rationale": "A real trap: today both legs happen to share the same GITHUB_WORKSPACE path shape, so `sha256sum output/images/*` would coincidentally work — but that's fragile, and the comment explicitly says so ('happen to match today ... by construction instead of by coincidence'). A maintainer 'simplifying' this command by dropping the `cd` would reintroduce a hidden dependency on runner path layout. Warning stays inline; the path-coincidence explanation moves to docs.",
    "loadBearing": true
   },
   {
    "startLine": 163,
    "endLine": 168,
    "topic": "compare job section banner: purpose, no container/build/caches needed",
    "category": "keep",
    "docsSection": "",
    "replacement": "  # ==========================================================================\n  # compare — downloads both legs' hash manifests and diffs them. This job\n  # needs no container, no Buildroot, no caches: it exists purely to fail\n  # loudly (with both hashes printed) on any mismatch, and to print the\n  # single agreed hash on success.\n  # ==========================================================================",
    "rationale": "4 content lines, no incident or trap content, explains the job's minimalism in a way that's useful at a glance and not duplicated elsewhere. Within the terse budget.",
    "loadBearing": false
   },
   {
    "startLine": 198,
    "endLine": 199,
    "topic": "Diff step: compare hash+filename pairs only, ignore legitimately-differing a/b manifest names",
    "category": "keep",
    "docsSection": "",
    "replacement": "          # Compare hash+filename pairs only (ignore the a/b manifest\n          # filenames themselves, which legitimately differ).",
    "rationale": "2 lines, prevents misreading the awk normalization as a bug; already minimal.",
    "loadBearing": false
   }
  ],
  "totals": {
   "keep": 8,
   "move": 1,
   "split": 6,
   "drop": 0,
   "commentLinesBefore": 124,
   "commentLinesAfter": 55
  },
  "proposedDocSections": [
   "reproducibility-workflow",
   "input-vs-output-caches",
   "no-image-upload",
   "shared-build-recipe",
   "manual-trigger-only",
   "no-container",
   "hash-bare-filenames"
  ]
 },
 {
  "file": ".github/workflows/publish-db.yml",
  "blocks": [
   {
    "startLine": 1,
    "endLine": 15,
    "topic": "File banner: what publish-db.yml does and why it's the sole db.json publisher",
    "category": "split",
    "replacement": "################################################################################\n#\n# publish-db.yml — regenerate + publish db.json on GitHub Pages (TASKS.md\n# P4.5, ADR 0018). Sole publisher for this doc; malformed output can crash or\n# silently break the fleet's update path. See docs/ci.md#publish-db-overview.\n#",
    "rationale": "The Downloader-polling mechanics and the 'why this matters' narrative are pure background, not something a future edit needs staring at it — but the blast-radius warning (breaks the whole fleet's update path) is exactly the kind of thing that must survive at the code per the split bias for incident-flavored comments.",
    "loadBearing": true,
    "docsSection": "Publish db.json — overview & blast radius"
   },
   {
    "startLine": 16,
    "endLine": 35,
    "topic": "TRIGGER: release:published + workflow_dispatch, draft/publish coupling with release.yml",
    "category": "split",
    "replacement": "# TRIGGER: `release: types: [published]` (drafts don't fire it) + manual\n# `workflow_dispatch` w/ required `tag` (no \"latest\" default, never guesses).\n# Do not decouple from release.yml's draft/publish gate — see docs/ci.md#draft-published-coupling.\n#",
    "rationale": "This is THE trap: the human-approval gate depends entirely on draft releases not firing the published webhook, and this exact story is also told (independently) in release.yml around its 'Create draft GitHub Release' step. Consolidating into one docs/ci.md section stops the two files' prose from drifting apart while the one-line warning keeps a future editor from 'simplifying' the trigger.",
    "loadBearing": true,
    "docsSection": "Draft vs published release coupling"
   },
   {
    "startLine": 36,
    "endLine": 46,
    "topic": "Stable Pages URL requirement — always same site, always overwrites",
    "category": "split",
    "replacement": "# STABLE URL: always deploys to the SAME Pages site and OVERWRITES the prior\n# deploy — required so users set db_url ONCE (TASKS.md P4.5). See docs/ci.md#stable-pages-url.\n#",
    "rationale": "'It overwrites in place and never uses a per-release path' looks like a bug to someone unfamiliar with the P4.5 'Done when' requirement; the warning must stay so nobody 'fixes' it into a per-release path and breaks every onboarded downloader.ini.",
    "loadBearing": true,
    "docsSection": "Stable Pages URL requirement"
   },
   {
    "startLine": 47,
    "endLine": 52,
    "topic": "One-time manual Pages enablement prerequisite",
    "category": "split",
    "replacement": "# One-time manual prereq this workflow cannot do for itself: enable\n# Settings -> Pages -> Source: GitHub Actions. See docs/ci.md#pages-prereq.\n#",
    "rationale": "Actionable operational fact (what setting, where) stays inline since it's the first thing a human debugging a failed first deploy needs; the 'why deploy-pages can't do this itself' elaboration is pure background and moves.",
    "loadBearing": false,
    "docsSection": "One-time GitHub Pages setup prerequisite"
   },
   {
    "startLine": 53,
    "endLine": 61,
    "topic": "VERSIONING: ADR 0018 string-equality requirement between db.json and /MiSTer.version",
    "category": "split",
    "replacement": "# VERSIONING (ADR 0018): db.json's linux.version must equal /MiSTer.version\n# by exact string match, or devices never update or re-flash every run,\n# forever. See docs/ci.md#adr-0018-versioning.\n#",
    "rationale": "The two failure modes (never-updates vs. re-flash-forever) are the sharpest warning in the file and must stay at the code; the derivation mechanics are duplicated three more times in this same file (lines 63-68, 199-206, 248-253) plus release.yml — all four collapse into ONE docs/ci.md#adr-0018-versioning section rather than four retellings.",
    "loadBearing": true,
    "docsSection": "ADR 0018 — db.json/MiSTer.version equality"
   },
   {
    "startLine": 62,
    "endLine": 68,
    "topic": "ADR 0018 mechanism: tagged-commit date -> MISTER_VERSION -> archive name -> db version",
    "category": "move",
    "replacement": "# Version derivation mechanism (archive filename -> YYMMDD -> --version):\n# see docs/ci.md#adr-0018-versioning.\n#",
    "rationale": "Pure mechanism narrative with no standalone warning; it restates what the 'Derive db version' step's own inline comment (lines 199-206) already shows in code, and is the same story as the header paragraph above it. Wholesale move avoids a third copy of the same explanation sitting one paragraph away from a near-duplicate.",
    "loadBearing": false,
    "docsSection": "ADR 0018 — db.json/MiSTer.version equality"
   },
   {
    "startLine": 69,
    "endLine": 74,
    "topic": "Version source must NOT be SOURCE_DATE_EPOCH or publishedAt",
    "category": "split",
    "replacement": "# Do NOT derive version from SOURCE_DATE_EPOCH (constant) or `publishedAt`\n# (human click time, not build date) — see docs/ci.md#adr-0018-versioning.\n#\n################################################################################",
    "rationale": "Two named wrong-answers a future maintainer might reach for first (there's a SOURCE_DATE_EPOCH right there in reproducibility.yml, and publishedAt is right there in release.json) — the warning has to stay exactly where someone would be tempted to 'simplify' the derivation.",
    "loadBearing": true,
    "docsSection": "ADR 0018 — db.json/MiSTer.version equality"
   },
   {
    "startLine": 92,
    "endLine": 99,
    "topic": "concurrency: single publish in flight, never cancel mid-deploy",
    "category": "split",
    "replacement": "# One publish/deploy in flight at a time; never cancel mid-deploy (an older\n# run finishing last could overwrite a newer publish). See docs/ci.md#concurrency-single-pages-target.",
    "rationale": "The correctness risk (stale run finishing last and clobbering a newer publish) is the whole reason cancel-in-progress is false here unlike build.yml/reproducibility.yml's true — that asymmetry must be visible at the code, not just in docs.",
    "loadBearing": true,
    "docsSection": "Concurrency: single Pages deploy target"
   },
   {
    "startLine": 104,
    "endLine": 105,
    "topic": "Workflow-level least-privilege permissions note",
    "category": "move",
    "replacement": "# Least privilege; job elevates only what Pages deploy needs. See docs/ci.md#least-privilege-permissions.",
    "rationale": "Already terse, but the exact phrasing ('least privilege at the workflow level; the X job elevates...') is duplicated near-verbatim in release.yml's own permissions block — the two should point at one shared docs/ci.md explanation of the pattern instead of maintaining two copies of the same boilerplate.",
    "loadBearing": false,
    "docsSection": "Least-privilege permissions pattern"
   },
   {
    "startLine": 115,
    "endLine": 119,
    "topic": "Job-level permissions: OIDC pages/id-token vs read-only contents",
    "category": "split",
    "replacement": "# OIDC-based Pages deploy (same mechanism as release.yml's attest-build-provenance);\n# read-only elsewhere — never touches the release itself. See docs/ci.md#least-privilege-permissions.",
    "rationale": "Cross-references release.yml's OIDC usage, so it belongs in the same shared permissions-pattern doc section as the block above rather than being explained twice in two files.",
    "loadBearing": false,
    "docsSection": "Least-privilege permissions pattern"
   },
   {
    "startLine": 167,
    "endLine": 169,
    "topic": "Draft-guard step: pointer to file header's coupling note",
    "category": "keep",
    "replacement": "      # See the file header's \"draft-vs-published coupling\" note: enforced in\n      # code, not just implied by which trigger fired, because\n      # workflow_dispatch could otherwise be pointed at a still-draft tag.",
    "rationale": "Already exactly 3 lines and already functions as a pointer in the house style (it references the file header, which after this refactor still carries its own short warning + docs/ci.md pointer) — no change needed.",
    "loadBearing": true,
    "docsSection": ""
   },
   {
    "startLine": 180,
    "endLine": 183,
    "topic": "Expect exactly one release_*.7z asset; fail loudly on 0 or 2+",
    "category": "split",
    "replacement": "      # Fail loudly, not silently, on zero or multiple release_*.7z matches\n      # (wrong builder or a naming regression). See docs/ci.md#release-asset-contract.",
    "rationale": "The 'fail loudly' intent must stay next to the jq count check it explains; the detail about which release.yml revision names the asset is background that belongs in the shared asset-contract doc section.",
    "loadBearing": true,
    "docsSection": "release_YYYYMMDD.7z asset contract"
   },
   {
    "startLine": 199,
    "endLine": 206,
    "topic": "Derive db version from filename, not publishedAt — inline bash comment",
    "category": "split",
    "replacement": "          # db.json version must equal /MiSTer.version (ADR 0018) -- derive from\n          # the filename, NOT publishedAt (differs from build/commit date).\n          # See docs/ci.md#adr-0018-versioning.",
    "rationale": "This is the second of four ADR-0018 retellings in this file; the operative warning ('from filename, NOT publishedAt') stays right on the sed line it governs, the YYMMDD/stock-format walkthrough moves to the single consolidated ADR 0018 doc section.",
    "loadBearing": true,
    "docsSection": "ADR 0018 — db.json/MiSTer.version equality"
   },
   {
    "startLine": 221,
    "endLine": 226,
    "topic": "Hash/size must come from the actual uploaded asset, not a pre-upload copy",
    "category": "split",
    "replacement": "      # hash/size must come from the ACTUAL uploaded asset, not a pre-upload\n      # local copy (this job has no access to one anyway). See docs/ci.md#asset-hash-from-live-download.",
    "rationale": "The one-sentence rule ('must hash the ACTUAL uploaded asset') is the part a future refactor could silently violate (e.g. by wiring in a build-time artifact instead of a fresh download); the 'runs in a separate workflow invocation' justification is background.",
    "loadBearing": true,
    "docsSection": "Hash/size from the live downloaded asset"
   },
   {
    "startLine": 248,
    "endLine": 253,
    "topic": "gen-db-json.py computes hash/size/version — third ADR 0018 restatement",
    "category": "move",
    "replacement": "      # gen-db-json.py computes hash/size/version for db.json (P4.5).\n      # See docs/ci.md#adr-0018-versioning.",
    "rationale": "Third occurrence of the same ADR-0018 story in one file (after the header and the 'Locate the asset' step); nothing here is a warning distinct from what's already stated twice above it, so it fully relocates.",
    "loadBearing": false,
    "docsSection": "ADR 0018 — db.json/MiSTer.version equality"
   },
   {
    "startLine": 268,
    "endLine": 274,
    "topic": "Schema self-check is a second, independent gate (TASKS.md P4.5 mandate)",
    "category": "split",
    "replacement": "      # Independent 2nd gate on the file about to be uploaded -- do not rely\n      # solely on gen-db-json.py's own check (TASKS.md P4.5 mandate).\n      # See docs/ci.md#schema-self-check-independent-gate.",
    "rationale": "'Independent gate, do not just trust the generator's own check' is the load-bearing instruction — it's what stops someone from deleting this step as 'redundant' with gen-db-json.py's internal validation. The db_entity_contract.py provenance detail (why hand-mirrored vs. live fetch) is reference material that belongs in docs.",
    "loadBearing": true,
    "docsSection": "Schema self-check as an independent gate"
   }
  ],
  "totals": {
   "keep": 1,
   "move": 3,
   "split": 12,
   "drop": 0,
   "commentLinesBefore": 123,
   "commentLinesAfter": 47
  },
  "proposedDocSections": [
   "Publish db.json — overview & blast radius",
   "Draft vs published release coupling",
   "Stable Pages URL requirement",
   "One-time GitHub Pages setup prerequisite",
   "ADR 0018 — db.json/MiSTer.version equality",
   "Concurrency: single Pages deploy target",
   "Least-privilege permissions pattern",
   "release_YYYYMMDD.7z asset contract",
   "Hash/size from the live downloaded asset",
   "Schema self-check as an independent gate"
  ]
 },
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 4,
    "topic": "File banner + one-line identity (\"weekly backport queue for ...\")",
    "category": "keep",
    "replacement": "################################################################################\n#\n# fork-sync.yml — weekly backport queue for MiSTer-devel/Linux-Kernel_MiSTer.\n#",
    "rationale": "Pure identification, already terse, nothing to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 5,
    "endLine": 11,
    "topic": "Why the queue exists (fork is live, nobody reconciles it, fork sat on 5.15.1 through 210 releases)",
    "category": "split",
    "replacement": "# Fork drifts silently unless asked; this job asks, weekly. See docs/ci.md#fork-sync-why.",
    "rationale": "This is foundational rationale with a concrete past-incident number (210 stable releases of silent drift) — exactly the kind of hard-won context the task says must not be lost, but it's a 'why does this file exist' story, not a warning that guards a specific line of code, so the narrative (including the 5.15.1/210-release detail) moves to docs while a sharp one-liner keeps a future maintainer from reading this as pointless overhead.",
    "loadBearing": true
   },
   {
    "startLine": 12,
    "endLine": 15,
    "topic": "What the job mechanically does (diffs fork-sync.conf vs live HEADs, opens an issue)",
    "category": "keep",
    "replacement": "# So: scripts/check-fork-sync.sh diffs docs/kernel-recon/fork-sync.conf (the last\n# RECONCILED commit per fork branch) against the fork's live HEADs, and this opens an\n# issue listing whatever has landed since. The queue becomes a fact rather than a memory.\n#",
    "rationale": "Short (3 content lines), purely descriptive of current behavior, needed immediately by a reader trying to understand the steps below — not a story to relocate.",
    "loadBearing": false
   },
   {
    "startLine": 16,
    "endLine": 23,
    "topic": "MiSTer-v6.18 special case: commits there must flow into linux-patches/ or linux-patches-upstream/ or `make export` silently erases them",
    "category": "split",
    "replacement": "# v6.18 commits need linux-patches[-upstream]/ or `make export` drops them silently. See docs/ci.md#fork-sync-v6-18.",
    "rationale": "This is THE trap this file exists to catch (per the original text: 'That is the failure this file exists to prevent') — a silent data-loss failure mode in `make export`. The sharp warning must stay directly above the code; the full explanation of PR #75, the two patch directories, and the README pointer moves to docs so it isn't lost, but a future editor scanning this file still gets stopped cold by the one-liner.",
    "loadBearing": true
   },
   {
    "startLine": 24,
    "endLine": 28,
    "topic": "COST: 2 API calls, no clone/build, why this can be a schedule not manual (contrast build.yml's 3h cold path)",
    "category": "move",
    "replacement": "# Cheap (~2 API calls, no clone/build) -- see docs/ci.md#fork-sync-cost for why this runs on a schedule.",
    "rationale": "Pure design/cost rationale for a decision already implemented in the trigger below (schedule + workflow_dispatch); it doesn't guard a specific line against a specific mistake, so it relocates wholesale behind a pointer. The build.yml comparison and exact commit-count context move with it rather than being lost.",
    "loadBearing": false
   },
   {
    "startLine": 29,
    "endLine": 33,
    "topic": "ONE ISSUE, UPDATED design: edit in place, don't open one per run (a muted queue = no queue)",
    "category": "split",
    "replacement": "# Update the existing issue -- do not open one per run (gets muted). See docs/ci.md#fork-sync-issue-queue.",
    "rationale": "This guards a real future edit mistake (someone 'simplifying' the issue-handling logic to just gh issue create every run) and the consequence is user-facing (notification fatigue) rather than obviously visible in a diff, so the warning must stay; the fuller 'people mute a job that reposts weekly' reasoning moves to docs.",
    "loadBearing": true
   },
   {
    "startLine": 39,
    "endLine": 41,
    "topic": "Cron schedule rationale: weekly matches actual fork commit rate (~11 commits in 2026), daily would spam",
    "category": "keep",
    "replacement": "    # Mondays 06:00 UTC. Weekly is matched to how fast the fork actually moves (11\n    # commits across 2026 so far) -- daily would just re-post the same list six more\n    # times before anyone acts on it.",
    "rationale": "Already terse (3 lines) with its concrete data point (11 commits) intact; nothing gained by relocating it away from the cron line it justifies.",
    "loadBearing": false
   },
   {
    "startLine": 45,
    "endLine": 45,
    "topic": "Permissions scope note: only issue write, no checkout/build",
    "category": "keep",
    "replacement": "# Only the issue write. No checkout of anything but this repo, no packages, no build.",
    "rationale": "Single line, already terse, directly documents the permissions block it precedes.",
    "loadBearing": false
   },
   {
    "startLine": 64,
    "endLine": 73,
    "topic": "Exit code contract (1=drift/ok, 2=error/must-fail) + past incident: double-invocation with `|| true` swallowed real errors and could file empty-body issues",
    "category": "split",
    "replacement": "# Exit 1=drift (ok), 2=error (fail); call the script ONCE. See docs/ci.md#fork-sync-exit-codes (past bug).",
    "rationale": "Documents a real fixed incident ('An earlier cut called the script twice ... which was wrong twice over') with a specific silent-failure mode (empty-body issue posted while still green). This is exactly the pattern the task calls out to bias toward split: a sharp warning survives inline so nobody reintroduces the double-invocation, the full incident narrative and the two-ways-it-was-wrong analysis moves to docs.",
    "loadBearing": true
   },
   {
    "startLine": 93,
    "endLine": 94,
    "topic": "Non-empty body.md assertion tied to the run-once fix above",
    "category": "keep",
    "replacement": "          # A body we would post must not be empty -- that is the failure mode the single\n          # invocation is meant to close, so assert it rather than assume it.",
    "rationale": "Two lines, already terse, sits directly above the `[ ! -s body.md ]` guard it explains; it's the local justification for that specific assertion rather than the incident story (which now lives in the exit-codes doc section), so it stays.",
    "loadBearing": false
   },
   {
    "startLine": 112,
    "endLine": 112,
    "topic": "Label may not exist on a fresh repo; create idempotently",
    "category": "keep",
    "replacement": "          # Label may not exist yet on a fresh repo; create it once, idempotently.",
    "rationale": "Single line, terse, self-contained explanation of the next command.",
    "loadBearing": false
   },
   {
    "startLine": 116,
    "endLine": 117,
    "topic": "--limit 1 rationale: only first match used, default page of 30 is wasted API work",
    "category": "keep",
    "replacement": "          # --limit 1: only the first match is used, and the default page of 30 is API\n          # work we never look at.",
    "rationale": "Two lines, already terse, explains a flag choice right where it's used.",
    "loadBearing": false
   },
   {
    "startLine": 133,
    "endLine": 135,
    "topic": "gh issue create has no --json/--jq; parse URL, don't retry with || (would double-file)",
    "category": "keep",
    "replacement": "              # `gh issue create` has no --json/--jq; it prints the issue URL. Take the\n              # number off the end rather than retrying the command -- a `||` fallback\n              # around a create is how you end up filing two issues.",
    "rationale": "Three lines, already at the terse ceiling, and it IS the warning itself with no separate long-form story to extract — splitting would leave almost nothing inline while gaining almost nothing in docs, so it stays whole per the 'nothing may be lost' constraint.",
    "loadBearing": true
   }
  ],
  "file": ".github/workflows/fork-sync.yml",
  "totals": {
   "keep": 8,
   "move": 1,
   "split": 4,
   "drop": 0,
   "commentLinesBefore": 55,
   "commentLinesAfter": 25
  },
  "proposedDocSections": [
   "## Fork sync queue (fork-sync.yml)",
   "### Why this job exists (#fork-sync-why) — fork is a live repo nobody reconciles unprompted; it once sat on 5.15.1 through 210 stable releases before anyone noticed",
   "### The MiSTer-v6.18 failure mode (#fork-sync-v6-18) — once PR #75 merges upstream, commits landing on that branch are changes to OUR series; they must flow into board/mister/de10nano/linux-patches/ or linux-patches-upstream/ or the next `make export` silently erases them",
   "### Cost and scheduling (#fork-sync-cost) — two GitHub compare API calls, no clone, no build (~seconds, weekly); contrast build.yml's ~3h cold path; this is why it's safe as a schedule rather than a manual step",
   "### One issue, updated in place (#fork-sync-issue-queue) — a fresh issue every run gets muted, which is the same as no queue; find-by-label, edit in place, close when empty",
   "### Script invocation and exit codes (#fork-sync-exit-codes) — exit 1 (drift, expected) must not fail the job, exit 2 (real error) must; run the script exactly ONCE with --markdown for both log and issue body — an earlier version called it twice with `|| true` around the expected exit 1, which also swallowed exit 2 and could file an empty-body issue while staying green"
  ]
 },
 {
  "blocks": [
   {
    "startLine": 1,
    "endLine": 20,
    "topic": "File header: why an invalid renovate.json is a silent failure",
    "category": "split",
    "replacement": "################################################################################\n# renovate-validate.yml — an invalid renovate.json fails Renovate SILENTLY\n# (no error, no PR, dependency PRs just stop appearing). See docs/ci.md#renovate-validate.\n#\n# Run the same check locally before pushing:\n#   npx --package renovate -- renovate-config-validator renovate.json\n################################################################################",
    "rationale": "The header's core claim — a broken config produces zero visible symptoms, just dependency PRs quietly not appearing — is the entire reason this workflow exists and must survive at the code so nobody guts the job thinking it's redundant with `git diff`-style linting. The supporting detail (kernel/Buildroot/9 driver commits/firmware pin count, the 3-hour-build paths: scoping justification) is real but is the kind of narrative that belongs in one canonical writeup rather than repeated at the top of every file that touches Renovate. The local-repro command is kept verbatim since it's operational, not rationale.",
    "docsSection": "renovate-validate",
    "loadBearing": true
   },
   {
    "startLine": 53,
    "endLine": 56,
    "topic": "Why --strict is used (fails on warnings, not just malformed JSON)",
    "category": "split",
    "replacement": "      # `--strict` also fails on warnings — needed so a manager that silently stopped matching still fails CI.\n      # See docs/ci.md#renovate-validate-strict.",
    "rationale": "This is a trap-shaped decision: `--strict` looks like optional pedantry, and a future maintainer 'simplifying' the invocation could drop it without realizing that's exactly the flag that turns a quietly-broken (but syntactically valid) manager into a hard failure. The one-line warning keeps that consequence visible at the call site; the fuller explanation of what --strict changes moves to docs.",
    "docsSection": "renovate-validate-strict",
    "loadBearing": true
   },
   {
    "startLine": 60,
    "endLine": 71,
    "topic": "2026-07-18 zero-extraction incident — why shape validation alone is insufficient",
    "category": "split",
    "replacement": "      # Shape-only checks can pass while every manager extracts zero deps (silent, 2026-07-18 incident).\n      # So this step runs a REAL extraction. See docs/ci.md#renovate-validate-zero-extraction.",
    "rationale": "This is the named past incident the task flagged explicitly: 4 days green while all 18 custom managers extracted zero dependencies (unslashed managerFilePatterns treated as globs) and the kernel datasource silently returned no-result. That is exactly the failure class this whole workflow exists to catch, so the one-line warning + date must stay at the code to stop someone from ever believing the `--strict` shape check above is sufficient on its own. The full incident narrative (the JSONata single-element collapse, the specific mechanism, the --platform=local / token rationale) is preserved verbatim in docs/ci.md, not summarized away.",
    "docsSection": "renovate-validate-zero-extraction",
    "loadBearing": true
   },
   {
    "startLine": 77,
    "endLine": 78,
    "topic": "Debug log volume trap — never spool to disk",
    "category": "keep",
    "replacement": "          # NB: the full debug log runs to several GB — filter the stream, never\n          # spool it to disk, or this step fills the runner.",
    "rationale": "Already a sharp, terse (2-line) trap warning sitting directly above the pipeline it describes. Nothing to relocate — moving it would separate the warning from the exact `LOG_LEVEL=debug` line it's warning about.",
    "docsSection": "",
    "loadBearing": true
   },
   {
    "startLine": 96,
    "endLine": 96,
    "topic": "Datasource failures are invisible in extraction stats",
    "category": "keep",
    "replacement": "          # A datasource that returns nothing is invisible in the extraction stats.",
    "rationale": "One line, already terse, and it's the reason the very next `if` block exists (checking for 'Failed to look up' separately from the fileCount check above) rather than restating what the code does. Not stale, not redundant with the split block above it — that one covers custom-manager matching, this one covers datasource lookups, a distinct failure mode from the same incident.",
    "docsSection": "",
    "loadBearing": false
   }
  ],
  "file": ".github/workflows/renovate-validate.yml",
  "proposedDocSections": [
   "## renovate-validate.yml",
   "### Silent-skip failure mode (why this workflow exists)",
   "### --strict: why warnings must fail the run too",
   "### 2026-07-18 zero-extraction incident"
  ],
  "totals": {
   "keep": 2,
   "move": 0,
   "split": 3,
   "drop": 0,
   "commentLinesBefore": 39,
   "commentLinesAfter": 14
  }
 }
]
