# Stock image inventory (P0.3)

This directory holds a **full inventory per stock release**, one subdirectory each:

| Directory | Release | Kernel | Status |
|---|---|---|---|
| [`20250402/`](20250402/) | `release_20250402.7z` (2025-04-02) | Linux 5.15.1-MiSTer | Complete — original P0.3 run, including `busybox-applets.md` |
| [`20260907/`](20260907/) | `MiSTer-devel/Linux_Image_creator_MiSTer` @ `d4e3f51ec7fdd18116562d38bace9ef7dffe0f38` ("Release 20260907.", 2026-09-07) | Linux 6.18.38-MiSTer | Complete except `busybox-applets.md` (no `qemu-arm` in the authoring session — see that file for the recovered version string and what is still missing) |

Every file below is generated by a script in `scripts/inventory/` (see that directory's
`README.md`) — re-run it against any future release (or a Buildroot-built image) for
parity checking (P2.2's SONAME checker, P2.3's init parity, P3.3's module/firmware infra),
and write the output into a new `docs/stock-inventory/<release>/` directory rather than the
top level.

This feeds P0.5 (ABI contract), P0.7 (package mapping), P2.3 (init
parity), and P3.3 (module/firmware infra).

## Files (present in every release directory, except where noted)

| File | Item | Contents |
|---|---|---|
| `shared-libraries.md` / `-full.txt` | (a) | Every ELF shared object, SONAME, symlink chains |
| `binaries-needed.md` / `-full.txt` / `-union.txt` | (b) | Every ELF binary's `DT_NEEDED`; the stock `MiSTer` binary and `busybox` called out; the union of all NEEDED (P0.7's package-mapping input); dangling-dependency findings |
| `etc-configs.md` / `etc-init-scripts-full.txt` | (c) | Init scripts, inittab, fstab, smb.conf, sshd_config, wpa_supplicant, etc. verbatim; A8 user-file-restore regular-file check; default-credential posture |
| `firmware.md` | (d) | `/usr/lib/firmware` file list, sizes, vendor grouping, corrected file count |
| `busybox-applets.md` | (e) | BusyBox version + applet list (run under `qemu-arm`) — **20260907: version string only, no applet list; `qemu-arm` was unavailable** |
| `stock-linux.config` | (f) | Stock kernel `.config`, extracted via IKCONFIG |
| `stock.dts` | (f) | Stock device tree, decompiled from the DTB appended to `zImage_dtb` |
| `kernel-config-dts.md` | (f) | Regeneration method + verification that the script reproduces the two files above |
| `disk-usage.md` | (g) | Disk usage by top-level directory + package-ish breakdown |
| `modules.md` | (h) | The `.ko.xz` modules, dependencies, driver-family grouping, P0.4 class, owning Phase 3 task |

## 20260907 vs. 20250402 — headline deltas

Full method: `docs/kernel-recon/fork-sync-2026-09/PLAN.md` §9.2 (`W3-docs-refresh`). The
20260907 tree was assembled by extracting `rootfs.tar.bz2` and overlaying `modules.tar.gz`
(`--strip-components=2` into `lib/`), `firmware.tar.gz` (into `lib/firmware/`) and
`addon.tar` (into `/`) exactly as `create_img.sh` (in the release archive) does — so the
inventoried tree matches what `create_img.sh` would actually format onto a card, without
needing a real ext4 image or root.

| Metric | 20250402 | 20260907 | Note |
|---|---:|---:|---|
| Kernel modules (`.ko.xz`) | 52 | **89** | All six out-of-tree Realtek Wi-Fi forks gone; mainline `rtw88`/`rtw89`/`mt76`/`ath*` USB set in |
| Firmware regular files | 66 | **91** | See `docs/firmware-parity.md` / `docs/bluetooth-parity.md` for which claims this flips |
| Shared ELF objects (total / SONAME-bearing / true ABI libs) | 1039 / 534 / 507 | **1039 / 534 / 507 — unchanged** | Buildroot userland was not rebuilt for this release; see below |
| ELF binaries | 536 | 535 | The 20260907 run had no separately-shipped `MiSTer` binary to fold in as an extra ELF (`docs/reference-materials.md`'s artifact list for this release has none); the 1-binary/1-SONAME gap this opens is that, not a rootfs change |
| Distinct `DT_NEEDED` SONAMEs | 251 | 250 | Same caveat |
| Rootfs content size (apparent, `os.walk`/`stat`) | 267.2 MiB (10,830 files) | **294.4 MiB (10,896 files)** | Firmware/module growth |
| BusyBox version | 1.33.1 (built 2022-12-24) | 1.33.1 (built 2026-07-22) | Same release, rebuilt; applet list not re-verified (no `qemu-arm`) |

**The Buildroot userland (everything outside the kernel/modules/firmware) is, by this
evidence, unchanged: Buildroot 2021.02.4, glibc 2.31, OpenSSL 1.1.1k, OpenSSH 8.6p1, Samba
4.14.6, Python 3.9.6, p7zip 16.02, and all 507 true-ABI shared libraries are byte-for-byte
the same set as 20250402** (same SONAME counts, same `/etc/os-release`). Release 20260907
replaced the kernel, its modules and its firmware only — see `README.md`'s top-level
comparison table for where each of those numbers is cited from.

## Headline findings, 20250402 (full detail in `20250402/*.md`)

- **Firmware count corrected: 66, not 72.** PLAN.md §3/§4.1, TASKS.md A5,
  and the verification doc all say stock ships "72 firmware files."
  The actual count of **regular files** under `/usr/lib/firmware` is
  **66**; `find /usr/lib/firmware | wc -l` (no `-mindepth 1`) returns 72
  because it also counts the firmware directory itself (+1) and its 5
  vendor subdirectories as if they were files. Confirmed independently via
  `find -type f` on the extracted tree and cross-checked with
  `debugfs -R "ls -l /usr/lib/firmware"` directly against the raw ext4
  image. **This doc's 66 is the authoritative count going forward** — see
  `firmware.md`. `xow_dongle.bin` (xone/xow dongle firmware) is present in
  stock's firmware set — see `firmware.md`'s redistribution callout for
  P3.2.
- **`/etc/resolv.conf` is a symlink, not a regular file** — one of A8's six
  user-file-restore destinations. `/etc/resolv.conf -> ../tmp/resolv.conf`
  (`/tmp` is tmpfs). Traced through `Downloader_MiSTer`'s actual restore
  code: the plain `open(target, 'wb')` copy follows the symlink, so
  against an *offline-mounted, unbooted* new image the restored content
  lands at `/tmp/resolv.conf` **inside the persisted image**, which the
  `tmpfs` mount over `/tmp` then shadows on first boot — the "restore
  custom resolv.conf" step is a no-op in stock, not a working regular-file
  copy. The other five destinations are genuine regular files. See
  `etc-configs.md`.
- **`/etc/init.d/S45bluetooth` is a symlink to `/bin/bluetoothd`**, not an
  inline script like every other `S`-script. `/bin/bluetoothd` is itself a
  conventional start/stop control script and is stock's actual Bluetooth
  pairing-persistence mechanism: it loop-mounts a small dedicated ext4
  image (`/media/fat/linux/bluetooth`) onto `/var/lib/bluetooth`. Directly
  relevant to P2.4's writable-paths audit and P3.5's Bluetooth parity task.
  See `etc-configs.md`.
- **Default-credential posture**: `root` is the only account with a
  login-capable password hash (every service account is locked); combined
  with `sshd_config`'s `PermitRootLogin yes`, stock ships remote root login
  gated only by a fixed default password. Reported factually (account
  structure, not the hash value) per P3.7/P4.8's needs — see
  `etc-configs.md`. SSH host keys are also baked into the image (same keys
  across every un-regenerated stock install) — same FAQ treatment applies.
- **3 genuine dangling `NEEDED` dependencies** (required but not provided
  anywhere in the image, by SONAME or by filename): `archivemount` needs
  both `libarchive.so.13` and `libfuse.so.2` (neither present);
  `libjack.so.0.0.28` needs `libdb-5.3.so` (not present). These binaries
  cannot actually load as shipped. See `binaries-needed.md`.
- **Kernel config regenerates byte-identical**; **DTS regenerates
  content-identical** (every node/property/value matches; the only textual
  difference is `dtc`-version-dependent cosmetic rendering of one line —
  see `kernel-config-dts.md` for the exact diff and why it's not a real
  difference).
- 507 true ABI shared libraries (SONAME-bearing, excluding ~27 bash
  loadable-builtins that technically carry a self-referential SONAME but
  are never `DT_NEEDED`-resolved); 251 distinct SONAMEs in the union of all
  `DT_NEEDED` across the image (the set P0.7 must map to Buildroot
  packages); 536 ELF binaries; 52 `.ko.xz` kernel modules exactly matching
  A5; BusyBox 1.33.1 with 274 applets.

## Headline findings, 20260907 (full detail in `20260907/*.md`)

- **89 `.ko.xz` modules** (vs. 52 in 20250402) — the six out-of-tree Realtek forks
  (`8188eu`, `8812au`, `8821au`, `8821cu`, `88x2bu`, and the sixth) are **all gone**;
  replaced by mainline's `rtw88`/`rtw89`/`mt76`/`mt76-usb`/`ath*`/`xone-*` set. Notable:
  **no `rtw88_8821au.ko.xz`**, **no cpufreq driver**, no `aic8800*` — matching
  `docs/kernel-recon/fork-sync-2026-09/PLAN.md` §1.1. See `20260907/modules.md`.
- **91 firmware regular files** (vs. 66) — now includes `rtw88/rtw8821a_fw.bin`,
  the `rtw89/*` set, `mediatek/mt7925/*`, `ath10k/QCA9377/*`, and (new for stock)
  `brcm/BCM20702A1-0b05-17cb.hcd`, a Broadcom Bluetooth firmware file stock did not ship
  in 20250402 — see `docs/firmware-parity.md` and `docs/bluetooth-parity.md` for what this
  changes in each doc's "stock does/does not ship" claims. **Still no AIC8800 firmware.**
  See `20260907/firmware.md`.
- **Shared libraries, binaries, `/etc` layout, disk-usage breakdown by directory, and the
  dangling-`NEEDED`/resolv.conf-symlink/S45bluetooth findings from 20250402 all still
  hold** — re-run against this tree and unchanged in substance (counts above). No new
  dangling `NEEDED` dependency was introduced.
- **Kernel config**: 4,246 → 4,659 lines, 1,163 changed lines (version-line diff, not
  reproduced against a second source at this stage — see `20260907/kernel-config-dts.md`).
  **DTS**: 998 → 1,130 lines, a genuine content change (new/renumbered phandles), not
  merely cosmetic dtc-rendering drift like the 20250402 self-comparison was — see the same
  file for the honest accounting of why this one is not "content-identical".
- `busybox-applets.md` for this release has the version string only
  (`BusyBox v1.33.1`, built 2026-07-22) — **the applet list was not re-verified** because
  `qemu-arm` is unavailable in this environment. Do not assume it is unchanged from 274;
  re-run `scripts/inventory/gen-busybox.sh` with a `qemu-arm`-capable host to close this.

## Regenerating everything

```sh
scripts/inventory/run-all.sh \
  work/extracted/files/linux/linux.img \
  work/extracted/files/linux/zImage_dtb \
  work/extracted/files/MiSTer
```

`run-all.sh` always writes to the top-level `docs/stock-inventory/` (its output-directory
is not parameterized for items a–e/g–h; item f alone takes an `[output-dir]` argument) —
move the resulting files into a new `docs/stock-inventory/<release>/` directory afterward,
the way `20260907/` was produced (see `docs/kernel-recon/fork-sync-2026-09/PLAN.md` §9.2 for
the exact commands used, including how a release's `rootfs.tar.bz2` +
`modules.tar.gz` + `firmware.tar.gz` + `addon.tar` were overlaid into one directory tree
first, replicating the release's own `create_img.sh`, since every `gen-*.sh` here accepts
an already-extracted directory equally to a raw ext4 image).

See `scripts/inventory/README.md` for the full per-script breakdown, what
each requires, and the determinism guarantees these files rely on to stay
diffable across a future Buildroot-built image.
