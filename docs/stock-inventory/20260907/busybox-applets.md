# BusyBox applets (item e)

> Generated by hand for Release 20260907 (P0.3 item e). Do not hand-edit further without
> re-running the same method — see below.
>
> Evidence method: `scripts/inventory/gen-busybox.sh` requires `qemu-arm` (user-mode ARM
> emulation) to execute the actual stock binary and list its applets. `qemu-arm` /
> `qemu-arm-static` is **not installed in this environment**
> (`which qemu-arm qemu-arm-static` → nothing; `apt list --installed | grep qemu` →
> nothing), so the applet list and full multi-call banner could not be regenerated this run.
> The version banner below was recovered without execution, via
> `strings bin/busybox | grep 'BusyBox v'` on the same binary.
> Source: Release 20260907 rootfs (`rootfs.tar.bz2`, `bin/busybox`)

Version banner (via `strings`, not execution): `BusyBox v1.33.1 (2026-07-22 02:41:37 CST)`

Compare 20250402: `BusyBox v1.33.1 (2022-12-24 16:26:14 CST) multi-call binary.` — **same
BusyBox release (1.33.1)**, rebuilt 2026-07-22 (was 2022-12-24). Applet set unknown for
20260907 (not re-verified); assume unchanged pending a qemu-capable re-run of
`scripts/inventory/gen-busybox.sh docs/stock-inventory/20260907` (or an equivalent tree) —
**this is a real gap, not a "same as before" finding**, since a BusyBox rebuild can change
`.config` and hence the applet list even at the same version number.

To fill this in later: install `qemu-user` (or `qemu-user-static`) providing `qemu-arm`,
then re-run `scripts/inventory/gen-busybox.sh <extracted-20260907-rootfs-dir>` and move the
resulting `busybox-applets.md` here (see `docs/stock-inventory/README.md`).
