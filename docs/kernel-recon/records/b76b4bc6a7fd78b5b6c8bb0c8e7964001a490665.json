{
  "sha": "b76b4bc6a7fd78b5b6c8bb0c8e7964001a490665",
  "subject": "dualsense: leds config for player 6.",
  "author": "Sorgelig",
  "author_is_pr_contributor": false,
  "date": "2021-07-10",
  "subsystem": "hid",
  "change_type": "in-tree-code",
  "files": ["drivers/hid/hid-playstation.c"],
  "added": 5,
  "removed": 4,
  "is_backport_of_vanilla": false,

  "disposition": "misclassified",
  "carried_patch": null,
  "carried_mode": null,

  "upstream": {
    "vanilla_shas": [],
    "vanilla_file_line": "drivers/hid/hid-playstation.c:1701-1709",
    "vanilla_quote": "static const int player_ids[5] = {\\n\\tBIT(2),\\n\\tBIT(3) | BIT(1),\\n\\tBIT(4) | BIT(2) | BIT(0),\\n\\tBIT(4) | BIT(3) | BIT(1) | BIT(0),\\n\\tBIT(4) | BIT(3) | BIT(2) | BIT(1) | BIT(0)\\n};\\n\\nu8 player_id = ds->base.player_id % ARRAY_SIZE(player_ids);",
    "equivalence": "contradicted"
  },

  "verification": {
    "method": "source-grep",
    "confidence": "high",
    "contradiction": true
  },
  "agrees_with_provenance_doc": false,
  "provenance_doc_ref": "patch-provenance.md:337",

  "impact": {
    "effect_if_absent": "Kernel-capability-only: the DualSense LED classdev's 6-element player_ids[] table (index 6 = BIT(4)|BIT(0)) and max_brightness=6 boundary check would not exist, so writing 6 to the `:player_id` LED would be rejected/clamped rather than lighting a 6th-player pattern. Zero user-visible effect today: Main_MiSTer/input.cpp:2702 hardcodes `set_led(led_path, \":player_id\", (num > 5) ? 0 : num)`, so it never writes 6 to this LED regardless of whether the kernel would accept it — the earlier num>7 clamp at input.cpp:2695 only bounds the DS4 color_code[] fallback path, not this write. No MiSTer user can reach player slot 6 on a DualSense today, with or without this commit.",
    "affected_hardware": ["Sony DualSense (PS5 controller)"],
    "device_ids": ["054C:0CE6", "054C:0DF2"],
    "severity": "cosmetic",
    "failure_mode": "silent"
  },
  "userspace_coupling": {
    "coupled": false,
    "interface": "LED class device `<hid-device-id>:player_id` brightness (fork extends valid range from 0-5 to 0-6 by growing player_ids[] from 6 to 7 elements and raising the clamp from `player_id > 5` to `player_id > 6` / max_brightness from 5 to 6). Kernel-side change only; Main_MiSTer not updated to use player 6.",
    "main_mister_ref": "Main_MiSTer/input.cpp:2702 `if (set_led(led_path, \":player_id\", (num > 5) ? 0 : num))` hardcodes a 0-5 clamp on this specific LED write, independent of the file's earlier `if (num > 7) num = 7;` at input.cpp:2695 (which only bounds the DS4 color_code[8][3] fallback array). Grepped input.cpp:2692-2729 (update_num_hw, the only writer of `:player_id`) and the whole tree for 'player.*6', 'max_brightness', '> 6' — no code path anywhere in Main_MiSTer ever passes 6 to this LED. coupled=false because this commit is pure kernel-side infrastructure with zero shipped userspace consumer."
  },
  "forward_port": {
    "applies_to_6_18": false,
    "conflicts": "Vanilla 6.18.38 uses a hardcoded 5-element player_ids array (indexed 1-5, no index 0) and modulo arithmetic (`player_id % ARRAY_SIZE(player_ids)`), not the fork's explicit boundary check. The fork's expanded 6-element array (with player_ids[0]=0 for off) and explicit boundary check are already absent in vanilla. This commit would require not only vanilla's fundamental player-LED architecture to be replaced with the fork's, but also the expansion to 6/7 players. Not forward-portable without wholesale LED subsystem re-implementation.",
    "effort": "high"
  },
  "dependencies": {
    "depends_on": ["f84543926371d7911bff2d3a12daa5d325e4c2d9"],
    "superseded_by": [],
    "duplicate_of": []
  },
  "license_provenance": null,
  "notes": "SONNET ESCALATION VERIFICATION — every claim re-derived from scratch:\n\n1. FORK DIFF (`git show b76b4bc6a` in /mnt/source/Linux-Kernel_MiSTer, +5/-4, drivers/hid/hid-playstation.c only): grows `static const int player_ids[6]` to `player_ids[7]`, appending `BIT(4) | BIT(0)` as the new index-6 pattern; changes the clamp `if(player_id > 5) player_id = 0;` to `if(player_id > 6) player_id = 0;`; and raises `ds->led.max_brightness` from 5 to 6 in ds_leds_create(). Indices 0-5 (off + players 1-5) are untouched byte-for-byte — this commit purely appends a 7th table entry and widens two bounds checks. Confirmed exact diff quoted verbatim above.\n\n2. VANILLA (/mnt/source/linux, HEAD detached at v6.18.38, `git status` clean): hid-playstation.c:1701-1709 hardcodes `static const int player_ids[5] = { BIT(2), BIT(3)|BIT(1), BIT(4)|BIT(2)|BIT(0), BIT(4)|BIT(3)|BIT(1)|BIT(0), BIT(4)|BIT(3)|BIT(2)|BIT(1)|BIT(0) }` (re-grepped and re-quoted directly, exact line-number and byte match to what was already on record) with `u8 player_id = ds->base.player_id % ARRAY_SIZE(player_ids)` — a 5-slot, 0-indexed, modulo-wrapping design with NO off/BIT(4)|BIT(0) 7th slot and no forward-portable expansion point. `git log --all -S'player_ids[6]' -S'player_ids[7]'` and `-S'BIT(4) | BIT(0)'` over the full vanilla history return nothing reachable from v6.18.38. equivalence=contradicted confirmed correct.\n\n3. MAIN_MISTER (re-grepped input.cpp fresh): update_num_hw() at input.cpp:2692 opens `if (num > 7) num = 7;` (line 2695) — but that clamp only bounds the DS4 `color_code[8][3]` FALLBACK array used in the else-branch (input.cpp:2709-2726) when the `:player_id` LED write fails. The actual `:player_id` write, line 2702, is separately and unconditionally clamped: `set_led(led_path, \":player_id\", (num > 5) ? 0 : num)`. This clamp is independent of and unaffected by anything in b76b4bc6a — it was already `> 5` before this commit existed and remains so today. No occurrence of 6 being passed to this specific LED write exists anywhere in Main_MiSTer (grepped '> 6', 'player.*6', 'max_brightness', all of input.cpp:2692-2729). CONFIRMED: player slot 6 is unreachable from shipped userspace, independent of whether this kernel commit is carried.\n\n4. DEPENDENCY / CARRY-DECISION COUPLING: b76b4bc6a's diff hunks are pure extensions of the `player_ids[]` array and `ds->led` classdev that f84543926 (`dualsense: add player id led control.`, full sha f84543926371d7911bff2d3a12daa5d325e4c2d9) introduced — confirmed by diffing f84543926's version of dualsense_set_player_leds()/ds_leds_create() against this commit's before-state, same symbols, same LED classdev, no intervening commit touches player_ids/max_brightness (checked 60e08955f and 0d60c3482 diffs directly: 60e08955f only edits mute-LED code inside the same function neighborhood — no player_ids/max_brightness hunks; 0d60c3482, the lightbar commit, greps clean for player_ids/player_id/max_brightness — genuinely unrelated feature, touches only :red/:green/:blue lightbar LEDs). Corrected dependencies.depends_on to list ONLY f84543926 (full sha) — the prior record's inclusion of 0d60c3482 was not substantiated by any functional link and is dropped here; 60e08955f's own record (dependencies.depends_on=[]) independently corroborates that lightbar/mute are not part of this dependency chain. carry decision rides on f84543926; if carried, player-6 extension is trivial to include; impact today none (no userspace reaches player 6).\n\n5. GROUPING / MISCLASSIFICATION: patch-provenance.md:337 (Class C table, re-read fresh) groups f84543926, 0d60c3482, 60e08955f, b76b4bc6a under two vanilla SHAs (8c0ab553b072, 8e5198a12d64) claiming all four are 'pre-upstream backports' now covered. f84543926's own record (records/f84543926371d7911bff2d3a12daa5d325e4c2d9.json) independently confirms equivalence=contradicted/disposition=misclassified for the base :player_id classdev — vanilla's 5-LED-keyed-to-gamepad-device architecture (8c0ab553b072) is a genuinely different, Main_MiSTer-incompatible interface. Since b76b4bc6a is a direct, single-purpose extension of that same classdev/array (not an independent feature), and vanilla has no 6/7-player expansion mechanism at all, b76b4bc6a inherits the same misclassification: provenance's 'dropped-upstream' claim does not hold for this commit either. disposition=misclassified confirmed correct.\n\n6. SEVERITY/FAILURE_MODE CORRECTION: prior record had severity='feature-loss', copying the framing from the base f84543926 commit (which DOES have current userspace coupling — Main_MiSTer actively writes :player_id for players 1-5 and silently falls back to a different visual cue on vanilla, a real behavior change today). This commit is different in kind: it ONLY adds capability for slot 6, which input.cpp:2702's hardcoded `(num > 5) ? 0 : num` clamp means Main_MiSTer never requests, with or without this commit being present. There is zero behavioral difference for any MiSTer user today, carried or not — corrected severity 'feature-loss' -> 'cosmetic' (matching the precedent set by the sibling 60e08955f/mute record, which used the same 'real kernel capability, zero current consumer, LED-only feature category' reasoning to land on 'cosmetic' rather than 'feature-loss' or 'none'). failure_mode='silent' confirmed correct and unchanged: dropping this produces no build/boot error on vanilla or in the fork — it's an inert table-size difference.\n\n7. No carried_patch: grepped board/mister/de10nano/linux-patches/*.patch for player_id/player_leds/max_brightness — only 0012-hid-fanatec.patch matches (max_brightness=1 for an unrelated Fanatec wheel LED, confirmed by its `--- a/drivers/hid/Kconfig` header — different driver entirely). Not carried.\n\nsonnet-verified: corrected — dependency chain narrowed to f84543926 only (0d60c3482 dropped, unsubstantiated), severity downgraded feature-loss -> cosmetic to reflect zero current userspace reach (input.cpp:2702 clamps this LED to 0-5 independent of this commit), disposition=misclassified and equivalence=contradicted both confirmed correct against a fresh vanilla re-derivation; carry decision for this commit rides entirely on f84543926's carry decision."
}
